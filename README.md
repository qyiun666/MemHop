<p align="center">
  <h1 align="center">MemHop</h1>
  <p align="center">
    <strong>Long-term memory for AI agents — a six-layer cognitive memory database in a single embedded file. Pure Go, zero infrastructure.</strong>
  </p>
  <p align="center">
    <a href="README.zh.md">中文</a>
    &middot;
    <a href="https://qyiun666.github.io/meowagent.github.io/">Website</a>
    &middot;
    <a href="https://github.com/meowagent/meowagent">MeowAgent (coming soon)</a>
  </p>
</p>

<p align="center">
  <a href="https://github.com/qyiun666/MemHop/actions/workflows/workflow.yml"><img src="https://github.com/qyiun666/MemHop/actions/workflows/workflow.yml/badge.svg" alt="CI"></a>
  <a href="https://pkg.go.dev/github.com/qyiun666/MemHop"><img src="https://pkg.go.dev/badge/github.com/qyiun666/MemHop.svg" alt="Go Reference"></a>
  <img src="https://img.shields.io/badge/go-1.27+-00ADD8.svg" alt="go">
  <img src="https://img.shields.io/badge/license-MIT%20OR%20Apache--2.0-blue.svg" alt="license">
</p>

<p align="center">
  <strong>Current: v1.6.5</strong>
</p>

---

MemHop is an **embedded long-term memory database for AI agents and LLM applications**, written in pure Go. It is not a vector database — it is a memory system modeled after how the human brain organizes knowledge, with identity, episodic recall, semantic compression, a knowledge graph and archival storage. One agent, one `.meh` file, zero infrastructure.

MemHop is an **agent-dedicated** memory database: each agent binds to exactly one `.meh` file, and a file-level exclusive lock guarantees a single instance per file (a second `Open` fails fast). It runs on **Linux, macOS, and Windows** with no cgo and no external service beyond your LLM endpoint.

Built as the brain memory of [MeowAgent](https://github.com/meowagent/meowagent) (coming soon), MemHop works as an embedded organ rather than a standalone service. No server to run, no configuration to manage — just open a file and your agent has memory.

> **Our stance on agent memory.** Memory should not be an afterthought bolted on with a vector database plugin or a plain-text log dumped into a context window. An agent without internalised memory is just a stateless function pretending to be intelligent. MemHop exists because we believe memory must be *cognitive* — structured, compressed, consolidated, and forgotten the way a human brain does — and *embedded* — living inside the agent process itself, not behind a network call. One file, zero infrastructure, a mind that grows with every conversation.

## Features

- **Six-Layer Architecture** — L0 Profile → L1 Engram → L2 Context → L3 Knowledge → L4 Archive → L5 Plan, with Dream consolidation
- **Scene-is-the-session memory loop** — one L2 scene = one host session. `Search` reads that scene's depth-1 topic set straight from the in-memory cache (zero LLM, zero embedding, no scoring) *and opens the turn*, and from then on the library is what holds both ids — the write calls name no scene and no topic. The host records the turn itself: `AppendArchive` writes what happened while it ran (dialogue originals and operation events being L4 content that differs only by `Kind`), and `Update` closes the turn with how it opened and how it ended, distilling its utterances into that topic's keywords in exactly one LLM call. Everything a turn holds lives under that one id, and the task tree it opened is L5. The scene's `FusedKeywords` set *is* the context a host injects
- **V2 Storage** — `.meh` format (`FormatVersion=0x0012`) with A/B dual headers, per-record CRC32 + torn-write truncation recovery, mmap zero-copy, snapshot/checkpoint. Record frames carry an 8-byte `agent_id` (26-byte header) and the engine indexes every record by `(agent, idHash)` domain. The L3 knowledge graph lives in the file-wide reserved shared domain, which holds nothing else. **Only `0x0012` opens** — files at `0x0011` or older are rejected, with no migration path: an older file's profiles carry no `agent_type`, so every domain in it decodes as the primary agent — not one wrong value somewhere but the same wrong value in every domain at once, against a rule that a file has exactly one
- **Multi-Agent Domains** — `Open(path, llm, defaults, profile)` → a DB handle whose domains are reached as handles, never as ids: `Primary()` for the domain the file was opened on, `SubAgent(llm, profile)` for one created under it and addressed by its name. Many agents share one `.meh` file with fully isolated per-agent domains (caches, Dream pipelines, domain locks); same-agent operations serialize, different agents run in parallel; idle domains reclaim memory on access cadence (`Defaults.AgentIdleTTLMs`) while their records stay on disk. One exception (below): the L3 knowledge graph is file-wide shared
- **L1 Scene Hypergraph** — Dream creates co-occurrence hyperedges between scenes whose keyword sets overlap (Jaccard ≥ 0.15, a floor the engine fixes rather than a knob) and decays/prunes them over time; an edge is weighted by the similarity it was born from and fades from there — it strengthens only when an endpoint's turn list came out different — a turn lost counting as much as one gained — so re-measuring a pair whose lists are unchanged, even after the same turns are re-distilled into different keywords, never undoes the fade. L1 is maintained by Dream for explicit graph queries and future association — reads never score or spread activation
- **Dream Pipeline** — consolidation over L0–L2 plus retention on both content and plan: L2 compress → index rebuild → L1 nodes/hyperedges rebuild → L1 decay → L0 distill (emotion/MBTI), with `l4_prune` (drops a topic's content past the retention window — seven days by default, `Defaults.ContentRetentionMs`) and `l5_prune` (drops plan nodes past it, exempting a tree still in flight) on every pass; returns a per-stage `DreamReport`
- **L3 Knowledge Graph** — multiple independent hypergraphs with node import carrying positional source refs and relation edges (an edge is its members plus its kind, so one node pair can hold several relations), graph deletion, keyword/type/id lookup that ANDs together, and BFS subgraph queries. The graph pool is **file-wide**: every agent domain of the file shares one L3 pool (project knowledge is imported once, visible to all), and the pool's lifetime is the file's, not any one domain's
- **Single Instance by Design** — one `.meh` file has exactly one owner: a cross-platform exclusive lock (linux/darwin/windows) makes a second open fail fast, and the embedded path runs with no server process and no background daemon
- **Minimal & Embeddable** — 3 direct Go deps (xxhash, go-openai, golang.org/x/sys) — **the engine contacts no embedding / vector service at all**, and there is no dimension to declare in the config; `sync.RWMutex` + `atomic.Pointer`, zero infrastructure

## Quick Start

> Full integration guide (config, all layer APIs, turns and trajectories, pitfalls):
> [INTEGRATION_GUIDE.md](INTEGRATION_GUIDE.md) · 中文: [INTEGRATION_GUIDE.zh.md](INTEGRATION_GUIDE.zh.md)

```go
import (
    "context"
    "log"
    "os"
    "time"

    memhop "github.com/qyiun666/MemHop/api"
)

db, err := memhop.Open(
    "agent.meh", // the whole database; no server, no dimension to declare
    memhop.LlmConfig{ // required, validated before the path is touched
        APIURL: "https://api.openai.com/v1",
        APIKey: os.Getenv("OPENAI_API_KEY"),
        Model:  "gpt-4o-mini",
    },
    memhop.DefaultMemHopDefaults,
    // Required for a file that is not there yet: opening one has to know whose
    // memory it is. An existing file keeps the primary it already has.
    &memhop.ProfileInput{Name: "my-agent", Role: "assistant"},
)
if err != nil {
    log.Fatal(err)
}
defer db.Close()

// One .meh file carries isolated domains, and they come back as handles rather
// than as ids. Primary is the domain the file was opened on; SubAgent creates
// (or returns) one addressed by name, optionally on its own LLM endpoint.
sess, err := db.Primary()
if err != nil {
    log.Fatal(err)
}

// Read memory = read one scene (a scene IS a host session), which also
// opens the turn about to run. An empty SceneID continues the domain's current
// scene — after a reopen it restores the one whose turn counter ran furthest —
// so a host running one agent over one library names no scene at all;
// NewScene: true is the only way to start a fresh conversation. Naming a
// SceneID scopes this read to it and it must already exist, otherwise
// ErrNotFound (L3ID anchors only a read that creates a scene). The read costs
// no LLM, no embedding and no scoring; NewTopicID is the topic this turn
// lives in.
res, err := sess.Search(memhop.SearchQuery{})
if err != nil {
    log.Fatal(err)
}
for _, topic := range res.Topics { // this session's depth-1 set = the context
    _ = topic.FusedKeywords
}

// While the turn runs, the host records what happened into the turn Search
// opened — which turn that is the library remembers, so this call names no ids
// and with no turn open it is refused. Dialogue and events are the same kind of
// record: they differ only by Kind. Each call returns the slot its record took.
_, _ = sess.AppendArchive(memhop.ArchiveInput{
    Kind:      memhop.KindEvent,
    EventType: "tool_call",
    Content:   `{"tool":"grep"}`,
    CreatedAt: time.Now().UnixMilli(),
})

// End of turn: Input and Output land on the dialogue slots a reader looks for
// them on (Seq 1 and 2), so closing the same turn again rewrites those two
// lines instead of accumulating; Outcome is the host's own word for how the
// round ended, stored as one event per call. The turn's utterances are then
// distilled into its keyword track, returned with the topic.
topic, err := sess.Update(memhop.TurnEnd{
    Input:     "What did we discuss yesterday?",
    Output:    "Agent: ...",
    Outcome:   "resolved",
    CreatedAt: time.Now().UnixMilli(),
})
if err != nil {
    log.Fatal(err)
}
_ = topic.FusedKeywords

// Dream consolidation (L0-L2); an empty sceneID sweeps every scene of the
// domain. Update already schedules it in the background once a scene's
// topic count passes the threshold, so hosts rarely call it.
report, err := sess.Dream(context.Background(), "")
```


> **Concurrency contract.** Same-agent operations (Search / Update / Dream / write APIs) are serialized by the library's per-agent domain lock; different agents run in parallel on a `*DB`, so the host needs no external queue. `*memhop.Session` carries no cross-domain state beyond its bound domain — the scene and turn it is working are held by the domain, not by the caller. The file's exclusive lock still allows only one process per `.meh` file; `DB` exposes no locking API: the domain lock is the library's, and a host critical section needs its own. Nothing about the open turn is process-wide, so one more agent is one more `Open` — including one made between another round's start and its close, which leaves each round's keys and content apart.

Prerequisites: Go 1.27+ and an OpenAI-compatible LLM endpoint (configured through `api.LlmConfig`, which `api.Open` takes) — no embedding / vector service needed

### API Overview

| Group | Methods |
|-------|---------|
| Core loop | `Search(q) → opens the turn` · `PlanNodeAdd(parentSeq, title) → seq` (parentSeq 0 opens the turn's tree) / `PlanNodeUpdate(PlanStep{Seq, Status, …})` (the plan comes first, one step at a time) · `AppendArchive(ArchiveInput{...}) → seq` · `Update(TurnEnd{Input, Output, Outcome, CreatedAt}) → topic` · `Dream(ctx, sceneID)` — none of the turn writes names a scene or a topic id: `Search` opens the turn they all key on |
| L0 Profile | `GetL0` · `UpdateL0` |
| L1 Engram (read-only) | `ListL1() → []SceneNodeView` — every scene node of the domain in a stable order, ids as hex. Dream builds the nodes and the co-occurrence edges between them and is the only writer, so there is no L1 write call. `Importance` / `Valence` / `Arousal` are what consolidation computed, and `EmotionSet` says whether any pass ever stamped the two signals (0 is a legal reading, so the values alone cannot); `EdgeIDs` has no read of its own — two nodes sharing one are a pair Dream judged related |
| L2 Context | `ListScenes([l3ID])` · `UpdateScene(id, {Name, L3ID, Force})` · `RenameTopic(topicID, name)` · `SceneContext(sceneID, or "" for the domain's current scene)` · `MergeScenes` · `DeleteTopic` · `DeleteScene` |
| L3 Knowledge | `GetL3` · `ListL3` · `ImportL3` (reports every graph the batch resolved a domain into, including one it added nothing new to) · `UpdateL3` · `DeleteL3` · `QueryL3Nodes` · `QueryL3Subgraph` (its `edgeKinds` narrows the walk; an undefined kind is refused, not answered with an empty subgraph) |
| L4 Archive | `AppendArchive(ArchiveInput{Kind, Seq, Role, ContentType, EventType, NodeSeq, Content, CreatedAt}) → seq` is the only way content enters a topic, and the topic it enters is the turn `Search` opened: the call names no ids, so a mistyped or invented key is not something a host can pass, and with no turn open it is refused (`ErrInvalidQuery`) rather than landing content under a key no read ever lists, `Seq: 0` allocates and the slot taken comes back, and naming a held slot rewrites it. An event's `NodeSeq` has to name a step this turn created (`0` binds it to nothing). `SearchL4(q)` is the one read over both kinds: keyword (case-insensitive), time range, ids, topic, `Kind` (utterance / event), `NodeSeq` (one plan step **and every step under it**, which means nothing outside its turn; `0` leaves the condition unset) and content type are conditions, not modes — an unset `Kind` selects both; `Limit` keeps the tail of the read's order (slot order inside one topic, record-time order across topics) |
| Turn events (L4, kind `event`) | A turn's events are L4 content of kind `event` under the topic id Search issued for it (swept past the retention window — seven days by default, configurable; no delete API); read them with `SearchL4(L4Query{TopicID, Kind: &KindEvent})`, append them with `AppendArchive`. A topic's first event is Seq 3, because slots 1 and 2 belong to its dialogue |
| L5 Plan tree | `PlanNodeAdd(parentSeq, title) → seq` · `PlanNodeUpdate(PlanStep{Seq, Status, Title, Summary})` · `PlanState()` — the tree is what L5 itself records: one node per record, addressed by the turn **plus a step ordinal the library hands out** (`1, 2, 3 …` inside that turn — issued above everything that still names one: this turn's live steps **and** the highest ordinal its surviving events bound to a step, since a step and its events share one address while the two age separately. Not permanently unique — a sweep frees a swept step's ordinal once the events naming it go too — and a turn's numbering can therefore carry a gap, so a host reads no density into it and treats an ordinal it held before, on a turn older than the retention window, as a new step's address); the turn is the one `Search` opened, so these calls name no id, which makes `PlanState()` and `SearchL4{TopicID, Kind}` one key across two stores, and `SearchL4{TopicID, NodeSeq}` reads back the work of one step and every step under it. A node exists only because something created it: a `parentSeq` of 0 opens the turn's tree, one naming a step the tree does not hold is refused rather than answered by growing one, and `PlanNodeUpdate` restates a step that is already there — `Status` every time (`in_progress` / `done` / `failed`, there is no "leave it" spelling), `Title`/`Summary` kept when left blank. A created step starts `in_progress`, so the model has no "planned but not started" state. A plan write stores no content: a step's events are L4 records the host appends itself |
| DB handle | `Open(path, llm, defaults, profile)` · `Primary()` · `SubAgent(llm, profile)` · `Checkpoint` · `CompactTo(newPath)` (defragmented copy; the destination is the caller's to constrain) · `Stats` (file bytes and live record count — the numbers a compaction decision is made from) · `Close` · `IsClosed` |

## Architecture

```
Layer   Name             Human Parallel          Mechanism
─────   ──────────────   ───────────────────     ─────────────────────────────────────────────
 L5     Plan             Task tree               One node per step, keyed by the turn that opened it; expired trees are swept by Dream
 L4     Archive            Turn content            A turn's dialogue originals and operation events (Kind), addressed by (topic, Seq); retention window (seven days by default) — the keyword track is what outlives it
 L3     Knowledge        Semantic memory         Multi-source hypergraph knowledge base
 L2     Context          Working memory          Topics on a scene's surface, plus turns Dream folded under a fused group (one sink per topic)
 L1     Engram           Scene hypergraph        Scene nodes + keyword-overlap hyperedges; maintained by Dream for explicit graph queries
 L0     Profile          Identity                Agent personality, preferences & language habits
```

### Dream Pipeline

The Dream cycle is an automatic consolidation pass inspired by how sleep processes the day's experiences. It acts on **L0–L2 only** (L3 distillation is out of scope) plus retention pruning over L4 content and L5 plan nodes:

1. **L2 compression** — the LLM groups related topics per scene; each target scene runs in its own goroutine under a bounded fan-out (the whole pass holds the domain lock, so the scene count is not the concurrency), sinking merged topics under a new depth-1 fused node
2. **L1 rebuild** — scene nodes are synced from L2, the L2Meta topic cache is rebuilt in the same scan, and keyword-overlap hyperedges are created or refreshed
3. **L1 decay** — scene importance and edge weights decay over time, weak nodes are pruned
4. **L0 distill** — emotion/MBTI and a personality summary are distilled from the ranked L1 samples and merged into the stored profile: `Name`, `Role` and `Preferences` are left alone, and `Personality` is the one host-written field Dream also refines, so a host write that omits it clears the refined value until the next pass. The per-node emotions that same reply carries are backfilled onto the L1 nodes that have never been stamped — a node already carrying a reading keeps it, including a legal all-zero one; the stage is skipped when there are no samples to read

Trigger: once a scene's depth-1 topic count passes `Defaults.SceneDreamTopicThreshold` (default 24), `Update` schedules that scene's Dream in the background, one pass in flight per scene; hosts may also call it. `Dream(ctx, sceneID) (*DreamReport, error)` holds the domain lock for the whole cycle, sweeps every scene of the domain when `sceneID` is empty (scenes below `DreamCompressMinTopics` are skipped) and honours `ctx` cancellation between stages.

### Read & write path

**There is no scored retrieval.** A scene is a host session, so the engine never guesses which scene a message belongs to:

| Path | What it does | Cost |
|------|--------------|------|
| `Search(SearchQuery{SceneID, L3ID, NewScene})` | empty `SceneID` → continue the domain's current scene (after a reopen it restores from the records, the one whose turn counter ran furthest), creating one only when the domain holds none; `NewScene: true` → a fresh scene, the only way to start a second conversation over one domain; a named `SceneID` → that scene's depth-1 topics (user-timestamp order) plus the L0 profile — and `NewTopicID`, the topic this read opens for the coming turn | in-memory read (L2Meta), zero LLM / embedding / scoring; the only write is the scene record (its turn counter) |
| `AppendArchive(ArchiveInput{Kind, ...}) → seq` | the turn's only content write, and what it writes into is the turn `Search` opened — which turn that is the library remembers, so this call names no ids and a mistyped or invented topic id is not even something a host can pass; with no turn open it is refused instead of landing content under a key no read ever lists. An utterance declares who spoke and what the content is; an event names itself and may hang on a plan step. `Seq: 0` allocates above the two dialogue slots, and the slot taken comes back | zero LLM; a refused record stores nothing, and an event naming a step the tree does not hold is refused rather than answered by creating one; budgets are 4 KiB per event record, name included, and 64 KiB per utterance, refused rather than truncated |
| `Update(TurnEnd{Input, Output, Outcome, CreatedAt}) → topic` | closes that turn: `Input` and `Output` land on its two dialogue slots (`Seq` 1 and 2), so re-closing a turn rewrites those lines instead of accumulating versions, and `Outcome` — the host's own word for which arm ended the round, which the engine never branches on — goes in as one `turn_outcome` event per call. It then distills the turn's utterances into its keyword track and returns the topic as stored, the track among its fields | exactly one LLM call per turn, and it runs before the topic is written, so a failure leaves no topic. A turn whose content the retention window already reclaimed is refused with `ErrInvalidQuery` without reaching the LLM, and so is this call when no turn is open |

What a host injects as context is the keyword set of that scene's depth-1 topics; to read a turn's original text, address L4 by that turn's topic id — `SearchL4(L4Query{TopicID, Kind: &KindUtterance})` — or use `SceneContext`, which already carries the messages. Content is bounded: past the retention window (seven days by default) a topic keeps its keyword track and its `Messages` come back empty or with a gap in `Seq`, which is a legal end state rather than a failed read. The injected size is kept in check by Dream converging each scene towards `DreamCompressMinTopics` (default 20) — a target the consolidation pass aims at, not a bound a scene is held to, so leaving automatic consolidation on is what keeps the context from growing without limit.

Removed along with retrieval: three-channel RRF scoring, L1 spreading activation (`AssociatedContexts`), topic centroids and the embedding dependency, the `AutoCreate` / `DirectedL2ID` / `DirectedL3ID` routes, and topic-level `L3Refs` (L2↔L3 now lives solely on the scene anchor `SceneSlot.L3ID`).


## Testing & Benchmarks

MemHop's test suite exercises only the public `api` surface — exactly the calls a host (e.g. MeowAgent) makes — and asserts the engine's own memory structures, not external answerability judges.

### Integration tests (`test/`, build tag `integration`)

- **Memory loop** (`TestCoreCycleUpdateDream`): N turns settled into one scene the way a real host does, with **periodic L0/L2/L4 consistency checks** every few turns — L0 profile readable, the scene read non-empty, L4 holding the raw utterance verbatim. After Dream consolidation the scene surface must shrink while every fact stays recoverable from L4. Inside one run this is a same-turn check: L4 content carries a 7-day window, so what Dream leaves behind long-term is the fused keyword track, not the text.
- **Keyword fidelity & persistence** (`TestKeywordFidelity`/`TestKeywordPersistence`/`TestDreamCompressionFidelity`): the keywords distilled from a turn faithfully carry its meaning, survive noise turns, and stay faithful across Dream compression.
- **API contracts** (`TestInterface*`: reads make zero LLM calls, writes cost exactly one distillation per turn, unknown scenes are rejected, checkpoints survive a restart), **e2e flows** (`TestE2E*`), **long-input robustness** (`TestExtractKeywordsLongInputRealLLM`/`TestUpdateLongTurnSettles`).

### Benchmarks (`go test -tags integration -bench .`)

All benchmarks drive the real api loop (real LLM, no external judge):

| Benchmark | Measures |
|-----------|----------|
| `BenchmarkMemoryLoop` | steady-state Search+Update loop including the engine's **auto-scheduled Dream** (a scene's depth-1 topic count passing the threshold) and periodic L0/L2 verification |
| `BenchmarkUpdateTurn` | one turn end to end: the read that opens it, then the close (the two dialogue writes, one distillation, the topic write) |
| `BenchmarkSceneRead` / `BenchmarkSceneReadLatency` | scene-read throughput and latency distribution (min/p50/p95/max) |
| `BenchmarkDreamConsolidation` | full Dream pipeline latency |


### Why no external dataset benchmark?

Public memory benchmarks (LoCoMo, LongMemEval) evaluate "retrieval → LLM-judged answerability" — a different question than what MemHop's layered design asserts (L0 profile distillation, L1 scene-graph coherence, L2 compression semantics, L4 verbatim archival). LongMemEval, the closest fit (multi-session user-assistant chats, ~500 QA), needs 115K–1.5M tokens per question and is not a practical continuous-integration target. MemHop therefore verifies its memory structures directly through the api loop instead of chasing a generic QA score.

## Project Structure

```
api/                         ← Public facade: open (the one entry) / session (the only
                               business handle, hex-id surface) / types / mapping / errors / exports
internal/ (root)             ← Big methods + composition root: config / db / session / models /
                               exports + agents / l0…l5 / l3query / search / update / dream
internal/scene|turn|dream|graph|content|plan
                             ← Layer-3 small methods, one package per cognitive face: none of them
                               takes the domain lock and none imports a sibling
internal/domain              ← Per-agent state: Context (domain lock, the three caches, OpCtx),
                               plan cache, L2Meta mirror maintenance
internal/config              ← Config types: the LLM endpoint and the tuning defaults a host supplies,
                               plus the bundle the assembly layer builds out of them
internal/llm                 ← OpenAI-compatible transport: Chat + the truncation-escalating retry
internal/cap/                ← Layer-4 capabilities, identity-neutral, dependencies injected:
                               engram / llmops / profile / knowledge
internal/repo/               ← Data layer: l0layer–l5layer + agentlayer (record read/write)
internal/repo/index/         ← Index layer: l2meta / rebuild (single-pass scan) /
                               l4 (the content each topic owns)
internal/repo/core/          ← .meh engine: engine / frame / header / snapshot / reclaim /
                               record / model / mmap / filelock
internal/common/             ← Bottom-layer utilities: enum / errors / hash / sliceutil / timeutil
test/                         ← Integration tests (build tag: integration): the offline host-face
                               suite and the real-LLM half
benches/fixtures/             ← Benchmark datasets (locomo10, locomo_smoke, longmemeval_smoke)
```

Dependency direction is strictly one-way: `api → internal → repo → core`, with `common` at the bottom (no references to any other internal package).


> Note: `docs/` and `AGENTS.md` are intentionally kept local-only (see `.gitignore`), so links under `docs/` may not resolve in a public clone.

### LLM Call Cost Model

- **Read path** (`Search`): **zero LLM, zero embedding** — served from the L2Meta cache alone.
- **Write path** (`Update`): exactly one keyword distillation per turn (both originals fed together), a 512-token output cap escalating on truncation, then one format-constrained retry — a reply that still will not parse is `ErrLLM` and that turn writes nothing.
- **Dream**: one consolidation call per scene reaching the topic floor (`DreamCompressMinTopics`, default 20), plus one distill call with at most 200 ranked L1 samples (up to 20 keywords each). Output caps: 8192 / 2048 tokens.
- Use a small/fast chat model (a cheap API model or a local OpenAI-compatible endpoint) for the configured LLM when latency and cost matter; keyword distillation does not need a frontier model.
- **The bill is per closing call, not per round.** Two closes of one turn are two distillations and still one topic — the dialogue slots keep the last pair of words while both endings stay as events on its track. A round that suspends for input and resumes is read and closed once per invocation, so it is two turns, not one closed twice.

## Development

```bash
go build ./...                          # Build
go vet ./...                            # Static analysis
go test ./internal/...                  # Unit tests (no external services)
go test -tags integration ./test/...    # Integration tests (requires an LLM key)
make check-guides                       # Compile the runnable skeleton both guides embed
```

Integration tests run against a real LLM (the engine needs no embedding service). Configure the LLM via environment variables `MEMHOP_TEST_LLM_KEY` / `MEMHOP_TEST_LLM_URL` / `MEMHOP_TEST_LLM_MODEL` (defaults to the DeepSeek endpoint when only the key is set), or via `test/testsupport/key_config.json`.

## Changelog

| Version | Date | Highlight | Core Changes |
|---------|------|-----------|--------------|
| v1.6.6 | 2026-09-23 | A turn closes in one call: the domain holds its own scene and turn, and the write shape carries no address |1. **`Settle` is gone**; one `Update(TurnEnd{Input,Output,Outcome,CreatedAt})` closes a round — Input/Output overwrite Seq 1/Seq 2 of the turn's topic, Outcome appends as a `turn_outcome` event per call (suspend + resume are two facts). 2. **Scene and turn ids now live on the domain**: `Search(SearchQuery{})` continues the domain's current scene and opens this turn (`NewScene:true` starts another conversation; recovery after a restart reads the scene's existing `turn_seq`), while `SceneContext("")` is a pure read that consumes no turn and never creates a scene (`ErrNotFound`) — only the round-opening read advances the counter, so a tool-calling loop cannot skip turns that never happened. 3. An anchor is adopted only by a read that creates a scene: handing `L3ID` while continuing a scene is refused, not ignored. 4. **Write shape split from read shape**: `AppendArchive` takes `ArchiveInput`, where the two ownership fields a read returns have *no place* to be passed (the `ProfileInput` standard), so writing onto a turn the host is not in is structurally impossible; writes are refused while no turn is open. 5. `DeleteScene`/`DeleteTopic`/`MergeScenes` clear the self-held halves, so a destroyed turn can never be written by the next `Update`; a turn key colliding with the reserved all-zero value is reported as corruption. 6. A host hex id crosses the boundary in exactly one place (`internal.FormatID` / `internal.parseID`); layers below no longer take id strings. Surface stays `Session` 25 + `DB` 7. 7. Millisecond timestamps are judged at the write boundary (second- and microsecond-scale values refused) — after the three-repo end-to-end run, the host performs no unit conversion at all. 8. `api.RoleDream` is exported again: a host reads a consolidated summary back and needs a name to tell it apart from a turn's own two lines, while the write boundary still refuses the role. 9. **Disk format `0x0012` unchanged, and this round’s semantics are pinned without spending quota**: the two self-held fields add no record type and no key; seven copies of the mock LLM server skeleton collapse to three, and `TestSecondLibraryOpenedMidRound` / `TestEachDomainHoldsItsOwnTurn` (a second open mid-round does not cross wires, each domain keeps its own turn), `TestDreamLeavesTheOpenTurnCloseable` (consolidation landing on an open turn neither drops it nor sweeps what that turn wrote) and `TestIdleReclaimRefusesTheDroppedRound` (idle reclaim crossing a turn’s middle refuses its writes and its close until the host reopens) pin the rest. 10. **Guide §6.5 gains the recall-port rule**: `SceneContext("")` answers `ErrNotFound` on a domain that has never been read, and a kernel that ends the invocation on a memory-port error would kill the first round of the agent that has nothing to remember yet — that one code becomes an empty answer; the same section records the measured read cost (300 topics in 1.4-2.2 ms, a per-topic follow-up at 43-64 µs), which is why no batch-by-topic read exists before it is proven needed. 11. **A fused group's parent id now includes its own members**: two disjoint groups over one pair of bounds no longer collide, so the loser is not left un-consolidatable forever — with it, every consolidation pass re-asked the model about that pair and refused it again, spending a real call for zero output. 12. **A plan ordinal is never reissued while an event still names it**: Dream's L5 sweep removes an expired step while the event bound to it survives (attribution is one field on an L4 record, and content ages on its own clock), so a new step in that turn inherited the swept step's history — issuing now takes a second floor, the highest ordinal named by that turn's surviving events, read from the L4 mirror; a turn's numbering can therefore carry a gap, so hosts must not assume dense ordinals. 13. **The recall fold became a stated contract** (§6.5 went from seven facts to eight): `SceneContext` flattens to depth<=2 on purpose — a fused group is the depth-1 row carrying its own summary as a `RoleDream` utterance, and the turns it swallowed are its depth-2 children, which no other read names — so a host drops `Depth>1`, takes the `RoleDream` line when `ChildCount>0`, otherwise the turn's own two lines, and dates a memory by its earliest message; get that wrong and one fact reaches the model twice, as the summary *and* as every turn it replaced. No shape, surface or format change. 14. **The single-instance lock is pinned at the facade too**: flock is held per open file description, so a second `Open` of the same path *in one process* is refused exactly like another process's — `ErrIO`, the holder untouched, and a different path opens fine alongside it (`TestOpenRefusesAFileThisProcessAlreadyHolds`; negative proof: neutering `lockFile` turns it red). Two agents sharing one path is the only collision the worker shape can meet, and what it asks of the host is another path, not a repair. 15. **Where project knowledge goes in the one-library-per-agent shape is now pinned at the facade**: L3 is a pool per *file*, so a worker opened on its own path starts from an empty graph while a worker created as a sub-agent domain of the parent's file inherits it with nothing to re-import (`TestKnowledgeGraphStaysInsideItsFile`; negative proof: scoping `ListL3` to the caller's domain turns it red). 16. **`make check-guides` compiles the runnable skeleton the guides embed**: §11's example is a host's fastest way in and nothing turned red when its symbols rotted; the target extracts each guide's `package main` block into a throwaway module with a local `replace` and builds it under `GOPROXY=off` (both guides build today; negative proof: renaming `SearchQuery` in the example exits 2 with `undefined: api.SearchQueryNope`). 17. **One vocabulary for the four tuning knobs: a knob left at 0 was not filled**, so the library default answers — read the way `LlmConfig`'s budgets already were. Turning one off now takes an explicit negative, and for the consolidation floor the negative is the *lossy* direction, since that number is what the prompt asks a scene to converge toward. The three-repo e2e runs on `MemHopDefaults{}`: all 14 checks still pass, and the merge it never asked for is gone. 18. **The recall contract gained the aged-group case**: content sweeps out while topic rows stay, so "skip depth-2, take the `RoleDream` line" would render an entire consolidated memory silent a week later while the group's own keyword track is still stored on its row — the fallback is that row's own `Keywords`, and it now covers any surface row that comes back with no messages, not just a group (the flattened listing carries no parent pointer, so whatever a reader falls back to has to be on the row). 19. **Every row of the scene read carries its own time bounds** (`SceneContextTopic.UserTimestamp`/`AgentTimestamp`, same names and meaning as on `TopicSlot`): running the host's own recall path past retention brings back four rows, every one of them message-less, and dating a memory by its earliest message then answers 1970 — which puts every aged memory first in line to be cut by a 'last N turns' window, while a plain turn whose two lines were swept was dropped outright. Those bounds were already on the record and this was the only read withholding them, so decision (4) now reads the row and scans nothing. Evidence on the host side: 1 entry dated 1970 becomes 2 entries dated by their own rows; the engine-side mapping assertion is new (negative proof: dropping the two map lines turns it red). 20. **An aged row's outcome word is now stated** (no shape change): what `Update` writes when it closes a turn — both utterances *and* the `turn_outcome` event — carries that one call's timestamp, so the retention window sweeps the outcome word together with the prose. A host's "did this round finish" field reads empty for exactly those rows, which means *unknown*, not *unfinished*; mid-round `AppendArchive` events age on their own clock, so an empty `Messages` list is still not "this turn recorded nothing" (measured on the host's own recall path: 2 entries, outcomes both empty). 21. **Decision (1) of the recall contract was made precise by measurement**: a later consolidation pass can fold away a group an earlier pass created, and that row keeps its own summary while sitting at depth 2 beside the turns under it — sinking takes a surface row down exactly one level per pass, so this read's depth cap cannot hide a topic (negative proof: sinking two levels leaves 1 of 5 rows listed). Skipping such a row is right because its words went into the newer summary, not because it was an original. New `internal/dream` case; surface and format unchanged. 22. **A round that has not settled is not in the scene read at all, and that is now stated**: the turn's topic row is created by `Update`, not by `Search` — the open only mints its id — so `SceneContext("")` lists settled rounds and nothing else. That is what makes recalling several times inside one round safe: a tool-calling loop never feeds its own half-written round back to the model, and whatever it appended arrives as that turn's own lines once the round closes (new case walks the whole order, with a positive control that the listing does see the settled turn; negative proof: creating the row at open turns it red). No production code changed<br>23. **A refused consolidation is re-asked, and the cost is now stated in the config table**: the trigger counts depth-1 rows and is checked once per settled round, while a refused pass changes nothing — so a scene the model never merges is still over the trigger at every round close. Measured by counting stub calls: 3 passes = 3 calls, i.e. one per scene per pass is the ceiling (a Dream already in flight for that scene absorbs the intervening closes) and the surface row count never moves, which rules out “it did compress, it just wasn’t recorded”. The other gate is measured too, and it does not bound the spend: `DreamCompressMinTopics` is checked *before* the model is asked, so a scene under it costs nothing — but the default floor (20) sits below the default trigger (24), and each settled round adds a row while the model keeps refusing, so raising the floor delays these asks rather than bounding them (it is also the prompt's convergence target). The single lever that bounds the spend is a negative threshold plus a `Dream` cadence the host chooses. Both guides carry it in the same config-table cell. No production code changed<br>24. **Engine costs now measurable without spending anything** (`test/benchmark_engine_test.go`, seven `BenchmarkEngine*` cases on the same stub endpoint the offline interface half uses): a settled round ≈18ms over a corpus of **3 scenes × 40 settled rounds** and — the number the single append-only file actually risks — **1350 bytes and 6 records per round**; pure scene read ≈0.2-0.3ms, the read that opens a turn ≈2.4ms, one mid-round record ≈2.0ms, a consolidation pass ≈19ms, reopening that 120-round file ≈0.4-0.6ms against ≈0.3ms for a file holding only its profile, which is what separates scanning the corpus from the fixed cost of an open — and the scan is a tenth of it. The first measurement of that last pair was wrong by 400× because the seeding ran inside the timed region; it was caught by the empty-file control plus a profile of an opens-only run, and the fix stops the timer before the corpus exists rather than adding a `ResetTimer`. A guard rejects a corpus that is not actually there (negative proof: multiplying its bound by 50 makes it report 722 records for 120 rounds), and each seeded corpus prints the shape it really built - scene count plus the rows the read under measurement actually addresses. Test helpers widened from `*testing.T` to `testing.TB`; no production code changed<br>25. **A domain that has never spoken now answers a recall with nothing instead of an error.** `SceneContext("")` used to refuse with `ErrNotFound`, which is the library declining to mint a scene for a write-free read - but "there is nothing yet" is something that read can say, and saying it as an error pushed the same special case onto every host: a decision-loop kernel reads memory before every model call and an error from that port ends the whole invocation, so the first recall killed a freshly recruited agent. It now returns an empty transcript with no scene named, which still separates it from a session that exists but has settled nothing (that one carries a name); naming a scene that is not there - including the zero id the library never issues - is still `ErrNotFound`, so the empty answer cannot swallow a real miss. Evidence: the 7-line special case in the swapped-in backend was deleted and the host memory packages stay green; negative control: reverting that one engine branch turns the host fresh-file recall red. One added return value on an internal helper; method counts and on-disk format unchanged<br>26. **"a host-visible list is always `[]`" became a gate, not a promise** (`api/surface_shape_nil_test.go`): the rule held only because the facade's single `mapSlice` happens to allocate, so breaking it made nothing red. The test walks every read's returned shape twice - against a database nothing has been written to, where a nil is most likely to escape, and again after a settled round, a plan step and a graph exist. Negative proof: building `mapSlice` with `var out []U` plus `append` makes it name seven leaks (`ListL1`, `ListScenes`, `ListL3`, `SearchL4`, `Search.Topics`, `PlanState.Roots`, and `ListL1` again on the populated pass). No production code changed<br>27. **Listing order became part of the answer** (`internal/repo/core/engine.go` + `api/surface_order_test.go`): the engine handed its index snapshot to iterators without sorting it, and the snapshot comes off map keys - so `ListScenes` returned a different order on every call against the same file. Fixed at the single place that yields ids (ascending), which settles every enumeration at once (`ListScenes`, `ListL3`, `QueryL3Nodes`, a graph's nodes and edges, an unfiltered `SearchL4`); callers that want another order still sort it themselves. The new test takes each of 12 pure reads eight times and requires byte-identical encodings; negative proof: dropping the sort makes it report `ListScenes answered differently on repeat 1`. Id order is not a host's notion of relevance, it is the "same answer every time" itself, and the cost is invisible in measurement (a round ~20.1ms / 1352 B, a pure scene read ~0.29ms, a reopen ~0.54ms - unchanged from before). Surface shapes and disk format unchanged<br>28. **The guides stopped telling hosts to copy a table they do not need**: `MemHopDefaults` is four knobs and an unfilled one takes the library default, so the shortest correct call is `api.MemHopDefaults{}`. The old wording ("copy `DefaultMemHopDefaults` and edit the copy", plus a stale "three business knobs" count) implied a required step and miscounted the struct - and made a host wonder whether leaving it empty switched something off. The claim is pinned at the entry point by `TestOpenTakesUnfilledDefaultsAsTheLibraryDefaults` (an all-zero table reaches the engine as exactly the default table); the host-side backend written against this library carried the same stale sentence and no longer does, with its three packages still green. Documentation only: no code, no shape changed<br>29. **A new gate: `api/guide_symbols_test.go`** - every `api.X` and every `handle.Method()` written anywhere in the two integration guides must exist on the published surface. The §11 skeleton only proved *its own* snippet compiles; prose and the per-layer tables carried unchecked names, and those are the lines a host copies. The symbol set is parsed live from this package's own non-test sources with `go/parser` (package symbols and methods), so the check cannot drift from what it checks. Today both guides resolve - 30~31 package symbols and 22 method calls each. The negative case is built in as a second test: a text naming `api.SessionContextTopic2`, `db.PlanCreate()`, `db.ListCapabilities()` and `sess.BogusMethod()` is reported symbol by symbol while `api.LlmConfig` and `db.Search` pass - retired and invented names fail on one rule. No production code changed<br>30. **Two pitfalls overstated the library, and both are measured now**: pitfall 5 said an abandoned round "leaves nothing behind" - it does not. The records it appended stay on the log (invisible to the scene read, unreachable through any id the library still names) until retention sweeps them, so abandoning a round that recorded something costs space, not nothing - `TestAbandonedRoundKeepsItsRecords` pins both halves. Pitfall 3 called a topic row's two time bounds the span of "its content" - they are the span of its **utterances**, because a mid-round event keeps a timestamp of its own; `TestTurnTopicBoundsIgnoreMidRoundEvents` pins that too (negative proof: folding events into the min/max answers 1000/9000 where 1000/1000 is right). Both are sentences a host sizes its prompt and its window on. No production code changed<br>31. **The shape promise now covers the encoded half** (`api/json_shape_test.go`): "a host-visible list is always `[]`" was pinned on Go values only, while what a host stores, forwards, or feeds a model is usually `json.Marshal` output - where an unset collection arrives as `null`, a third answer nobody asked for that a host re-decoding into its own type must branch on. Field names come from this package's own structs via `go/ast`, and only keys that **every** declaring type uses as a slice or map count, so an intentionally absent scalar (`parent_id`, the anchor) is not swept in. It runs over the same reads on a database that has settled nothing and after one round. Negative proof: dropping the facade's empty-map guard for `Preferences` makes it name `GetL0.preferences` and `Search.profile.preferences`. No production code changed<br>32. **A real corpus now round-trips verbatim in the offline interface half** (`test/api_interface_corpus_test.go`, no quota): one half of the fixture target needs no model at all - the QA answers are derived (`7 May 2023` appears nowhere in the source lines), so that half does; but "is it still the same set of turns after going through the loop" is the engine's own accounting. The test drives the first sample of `locomo10.json` (19 sessions, ~370 lines folded into 185 rounds) and asserts per session: row count equals settled rounds, both utterances byte-identical to what was written, row order is speaking order (timestamps non-decreasing), every row addressable with a non-empty keyword track, and - using the host's own recall rule - a line lifted from the middle round matches exactly that row. Negative proof: reversing the returned rows makes it report `round 0 message 0 came back altered`. The offline half is now 26 cases in 4.4s. No production code changed<br>33. **The facade document had holes, and discoverability is now gated**: `DB.Stats()`'s return type `DBStats` appeared **nowhere** in either guide (§8 lists the methods, §9's type table skipped it), and `DreamReport`/`DreamStage` were missing from that row too - a host that cannot read a type's name has to go dig through `go doc`, which is not "integrate and use". Both guides gained the rows, and `api/guide_symbols_test.go` now checks the pairing in reverse: **every exported type must be named in both guides**. Two negative proofs ran: the helper flags types a text omits, and deleting the word `DBStats` from the English guide fails the wired test with `never mentions [DBStats]` (restored afterwards; note an overlay adding a type to `api/types.go` cannot fool this check, because it reads the sources on disk, not the compiler's view). No production code changed<br>34. **The compacted copy now has to answer identically, byte for byte** (`test/api_interface_compact_test.go`, no quota): `CompactTo` rewrites the whole file and is the one path that can lose a layer quietly - the copy opens, answers and looks fine while a bucket, a counter or a domain was never carried over. The old check proved two things (the deleted scene is gone; the graphs survived). Now a 2-domain, 6-scene corpus (a record, a plan step and a close per round) is compacted and every read is compared across the operation - profile, L1 nodes, the scene list, each scene's whole transcript with its keyword tracks and both time bounds, the domain's archives, the L3 graphs - while the reachable record count must stay put (47 either side) and the file must actually shrink (19370 -> 17705 bytes); then one more round settles on the copy, gaining exactly one row, closing the round just opened, and minting an id absent from the pre-compaction snapshot, where a reset counter would collide. Two independent negative proofs: dropping a record type reports `copy reaches 22 records, want 47`; filing every record under the default domain with the count guard disabled makes the snapshot comparison itself say `answers the primary domain differently`. Writing it caught two mistakes of my own: comparing `Stats()` on the still-open file (which `CompactTo` never touches), and treating a leading `0000` in an id as suspicious - a one-in-65536 legitimate case, replaced by the real invariant. Offline cases 26 -> 27. No production code changed<br>35. **The deployed topology now has concurrency evidence** (`api/two_libraries_test.go`): one library per agent, a recruited worker opening its own `.meh`, and both handles driven at once by three goroutines (six rounds each: open, record, close). It asserts three things - every handle mints six distinct topic ids, the two files' id sets are disjoint (one family's rounds cannot appear in the other's file), and each handle reads back exactly its own six rounds - plus that the worker file holds strictly more records while the parent's count is unchanged after the worker ran the whole loop concurrently. `go test -race` is clean across three runs, and the two packages CI's race step does not cover were checked locally (`-race ./api/...`, `-race ./test/ -run TestInterface`, both green; CI races only `./internal/...`, widening it costs roughly 10s - a suggestion, not a change made here). No production code changed. Writing it, I first asserted the wrong thing: that the two files should reach equal record counts - the worker file runs two domains against the parent's one - replaced by the claim that actually shows non-interference.<br>36. **Anchor clearing on graph deletion now provably reaches a reclaimed domain** (`internal/anchor_detach_test.go`): the existing case covers a live tenant context and judges the detach only by `ListScenes(deletedGraphID)` coming back empty — an assertion that would also pass if the sweep deleted scenes. The new one covers what that cannot see: a domain whose context the idle sweep has already reclaimed must still be visited, scenes must survive having their anchor cleared, and the primary domain is in scope too. Negative proof: pointing `detachGraphAnchors` at the live-context map instead of the tenant registry makes the new test report `domain … still anchors scene … to the deleted graph` while the existing test stays green - blind. The check also settled a question (not a defect): the tenant registry is rebuilt from records at Open and is not trimmed by idle reclaim, which is why a reclaimed domain is not skipped - a property nobody had asserted. No production code changed.<br>37. **The guides now name the two clocks** (§12 item 3, both languages): the content a host records and the round it closes carry *the caller's* millisecond stamp - a missing `CreatedAt` is refused rather than filled in, because that value is the retention window's ruler - while what the library derives (a plan step's timestamps, a profile's `UpdatedAtMs`, a graph's content clock) it stamps itself on the same scale. The distinction is a port-shape fact, not trivia: a decision-loop memory port typically passes no time at all (`Remember(facts)`-style), so an adapter must bring a clock, and a first host reading silence would assume leaving it out was fine. Both timestamp tables therefore gained an absent-stamp case (`0` and a negative) and the two tests were renamed to `...RefusesAnAbsentOrWrongUnitTimestamp`. Negative proof: making `checkTimestamp` stamp the current millisecond when the caller gave none turns both new cases red with `no stamp at all (0): want ErrInvalidQuery, got <nil>`. No production code changed.<br>38. **A merge now has to survive reopening the file, which nothing checked before** (`test/api_interface_merge_reopen_test.go`): merging is the one correction that *rewrites record ownership* (every moved topic's `scene_id` becomes the surviving scene's), while the existing case only inspects the live answer - moving caches and leaving records alone would still pass it. The new test settles two rounds in scene A and one in B, merges, closes the file and reopens it: one scene listed, three rows, all six utterances verbatim, a non-empty keyword track per row, and the merged-in turn's two archives still reachable under its own topic id. Negative proof: teaching `MergeScenesL2` to move only the caches and write the topics back unchanged makes the new test report `the surviving scene lists 2 rows after reopen` while `TestInterfaceMergeScenes` stays green - the blind spot was real, not hypothetical. No production code changed.<br>39. **Deleting a scene must leave a file indistinguishable from one that never had it** (`internal/delete_scene_orphans_test.go`): no existing test would catch a cascade that misses a layer, because they all inspect the live answer - a record the caches stopped showing but the log still holds simply returns on the next reopen, when indexes are rebuilt from records. So the expectation is arithmetic, not a belief about what the cascade touches: file X drives two scenes (two rounds, then one) and deletes the first, file Y drives only the surviving scene, both are closed and reopened, and the four readable layers must match item by item (scenes, topics, archives, plan nodes). L1 is excluded on purpose: a deleted scene's co-occurrence hyperedges are pruned by the next Dream's decay pass, documented as such, and are not a record any host reads back. Negative proof: skipping the plan-node step in `DeleteCascade` makes it report `deleted-file {planNodes 3} vs never-had {planNodes 1}`. No production code changed.<br>40. **Two graph boundaries now fail loudly when broken** (`internal/graph_scope_test.go`): a relation names its far side by title and resolution stays inside the item's own graph - that scoping is exactly what makes deleting a project domain safe, because an edge reaching into another graph would leave the survivor's subgraph walk naming a record that cannot be read, and this library answers that divergence with a hard ErrIO rather than a skipped row. The test asserts three things together: a cross-graph relation is refused per item, the asking graph keeps no edge for it, and after deleting that graph the file matches - across a reopen, item by item - one that never imported it (graph slots, nodes, edges). Mutants bite in the right places: widening only the membership lookup to other graphs fails with `a relation naming a title from another graph was accepted`, while deleting the graph's nodes and edges but not its slot reports `the pool holds [4 2 1], while the file that never had it holds [1 2 1]` (an earlier mutant also polluted the edge dedupe keys and lit up the control file instead - not targeted evidence, so it was redone). Two of my own assumptions were corrected on the way: graph ids are not returned in a guaranteed order, so the sweep is now 'delete every graph but the one under test'. No production code changed.<br>41. **Deleting a topic now has to take the records its children own, not only the named one's** (`internal/l2_test.go`, widened): the old assertions checked both topic records and the parent's archive, so a mis-computed closure would leave a child's archives and plan steps on the log - unreachable by any read, yet still counted and compacted. The test now gives the child an archive and a plan step of its own and requires them gone too. Negative proof: narrowing `DeleteTopic`'s closure to the named id alone lights up four assertions at once (the child's record, its cache entry, its archive, its plan node) while `TestDeleteTopicSubtreeComesFromParentID` stays green - it inspects whether children disappear from the listing, not who owns their records, so this is a different blind spot rather than a second coat of paint. No production code changed.<br>42. **A consolidated scene now has to read the same after a restart** (`test/api_interface_consolidate_reopen_test.go`, no quota): consolidation is the one pass that changes what a scene looks like without deleting anything - merged turns sink to depth 2 and a group row appears at depth 1 - and the listing a host reads comes from a cache Dream maintains incrementally, so a cache/record split shows up as the same file answering differently before and after a restart. The test runs one pass, then closes and reopens (indexes rebuilt from records) and requires byte-identical snapshots; a guard inside the snapshot demands at least one group with children and at least one sunk child, or the comparison would be vacuous. Two mutants light the two layers separately: dropping each turn's user line in the L4 mirror rebuild fails at the byte comparison (only the reopened answer is missing a row - the accident shape this is built to catch), while dropping depth>1 rows in the L2 rebuild fails at the guard. It also corrected one of my assumptions: I expected a cache-only sink to look fine live and break after reopen, and it broke both, so the listing is not purely cache-driven - the invariant holds more firmly than assumed. Offline interface cases 28 -> 29. No production code changed.<br>43. **All three keys of the scene read's row order are now asserted, and the guides say it out loud for the first time**: the listing is sorted by (user timestamp, shallower first, topic id) - the second key is not decoration, because a fused group carries the timestamp of the first turn it swallowed, so ties are the normal case, and the host's collapse rules read the listing linearly (a group landing among its own originals summarises nothing). Only the primary key had a test; the tie-break lived in an implementation comment and the module doc, while neither guide's `SceneContext` row mentioned order at all - so a host had to re-sort, and the backend draft did exactly that. The consolidation reopen test now checks all three keys and demands that a tie actually occurred, or the secondary keys would go untested. Negative proof: flipping the depth comparison fails with `at a timestamp tie the deeper row leads at 1, so a group does not introduce its own originals`. Both guides now state the guarantee, and the draft's redundant `slices.SortFunc` is gone with its three packages still green. No production code changed.<br>44. **The guides' runnable skeleton is now executed, not just compiled** (`test/guide_skeleton_test.go`, offline, runs with `go test ./test/` and therefore in CI): `make check-guides` only builds the embedded program, which proves names line up - but a host follows the document, not the tests, and a quickstart that compiles and then hits `log.Fatal` on its first call is invisible to a build. The new test extracts the same `package main` block by the same rule, drops it into a throwaway module the way a real host would (`replace` to this repo, `GOPROXY=off`, dependencies from the module cache), builds it and **runs it** against the package's own stub endpoint: four environment variables point at the mock LLM and a temporary `.meh`, and the program must exit 0 with no `panic:` in its output; each guide is its own subtest. Negative proof: setting one skeleton `Update`'s timestamp to 0 - exactly the boundary §12 states as 'the library never stamps for you' - fails with `a positive timestamp is required` and exit status 1; the guide was restored and verified clean afterwards. No production code changed.<br>45. **Every exported facade method now has to carry a doc comment** (`api/facade_docs_test.go`): `internal` is not published, so `go doc .../api.Session` is the only text a host can read - an undocumented method is one a host cannot learn from the documentation, the opposite of "integrate and use". The scan parses this package's own sources (same route as the guide-symbol gate), requires a doc comment on every exported method of the two handles, and pins the number walked at 32 (25 + 7) so a parse that finds nothing cannot pass. All 32 are documented today. Negative proof: deleting the seven-line comment block above `RenameTopic` reports `(Session).RenameTopic is exported but carries no doc comment`; restoring it goes green again - and note the first attempt used `-overlay`, which this check cannot see because it reads files from disk, so it passed falsely until the mutation was made real. Same round: AGENTS' mechanically checkable claims were verified against the code (3 direct dependencies, `RecordHeaderSize = 26`, `FormatVersion = 0x0012`, `SnapshotVersion = 0x03`, unix/windows lock files present), and the three-repo e2e plus the host's swapped-backend tree were re-run at the current commit, both exiting 0. No production code changed.<br>46. **Every backticked identifier in the guides now has to resolve** (third check in `api/guide_symbols_test.go`): the symbol gate covers `api.X` and `handle.Method()`, but prose and the tables name things bare - fields, constants, enum modes - and those are exactly where a rename leaves a document quietly lying. The scan builds the legal set from this package's and `internal`'s sources (package names, methods, struct fields, plus test function names, since a guide citing the case that proves a claim should stay allowed) and requires every capitalised backticked token to appear in it; the only allowlisted names are the two the guides mention to say they do not exist. It caught two host-breaking shorthands on the spot: the enum list read `L3ImportSkip` / `Merge` / `Overwrite` where the real constants are `L3ImportMerge` / `L3ImportOverwrite` - a host typing `api.Merge` does not compile - in both languages, and a status word written as `Done`, which is no constant at all. Negative proof: renaming one entry to `L3ImportMerged` makes the gate name it. No production code changed; both guide skeletons still build and run.<br>47. **The two LLM budgets' "unfilled means default" is now an assertion** (`internal/llm/provider.go` extracts `budgets`, `internal/llm/provider_test.go` pins it): an unset `TimeoutSecs` answers 120 seconds and an unset `MaxOutputTokens` answers 8192, which the facade's own alias comment promised but no test held. Both failure modes are silent - a zero output ceiling truncates every reply to nothing, and a zero HTTP timeout does not mean "immediately" but "forever", so one wedged endpoint would keep a domain's lock held. Extracting the pure function is testability only: behaviour and the published surface are untouched (this go-openai version exposes no HTTP-client accessor, so the assertions sit on `budgets` and `Provider.MaxOutputTokens()`). Guides §4's two rows - which read "—" in English and "否" in Chinese, saying nothing about what omitting does - now state 120 / 8192 and that leaving them out is the normal case, and the llm package doc records the rule.<br>48. **Retention sweeps by the stamp, not by the round's state - which the docs had backwards** (`internal/dream_midturn_test.go` + three doc fixes): the existing case only ever tested an in-window mid-round record (stamped "just now"), and the module doc and AGENTS both generalised that into "Dream does not sweep what the open round already wrote". Measured: with `ContentRetentionMs` at 1s, a record appended into the open round with an hour-old stamp is gone after one `Dream` - **expired is swept, open or not**; what the pass does not do is take the turn itself, so the pending `Update` still lands on that same round (its own pair carries a fresh stamp and survives), and the scene then reads back one row with two messages. Both directions now have a test, and the overstated sentences are narrowed. Negative proof for the new case: exempting the open round from the sweep (zeroing the cutoff while a turn is open) reports `the expired mid-round record survived the sweep` and leaves the in-window case untouched. No production code changed.<br>49. **A plan tree is exempt from the L5 sweep only on both conditions, and the guides advertised one** (`internal/dream/plan_prune_test.go` + pitfall 7 in §12 of both guides): the implementation spares a tree only when it is still in flight **and** was acted on inside the window (`internal/dream/prune.go`: `agg.HasNonDone && agg.LastActiveAt >= cutoff`), and inside a tree that is not exempt every step is measured on its own clock. The guides said "a tree still in flight is exempt", which a host reads as an abandoned tree living forever - the opposite of why the stage exists. Neither combination was tested: the existing case (`TestPrunePlanStageSkipsWhenTheTreeIsIncomplete`) covers only the exempt side. The new case plants three trees (in flight and recent / in flight but silent / finished with one stale and one fresh step) and reads back per tree: 1 step, 0 steps, 1 step (2 of 4 nodes pruned). **Negative proof**: turning the AND in that guard into an OR fails exactly the latter two assertions and leaves the first. No production code change.<br>50. **The graph slot's `updated_at` is a change clock, and that sentence is now asserted** (`internal/l3_graph_clock_test.go` + the missing half of the facade comment in `api/types.go`): a host that lists its project knowledge by recency reads exactly this field. An import batch keeps two sets - the graphs a domain resolved into (what `GraphIDs` reports) and the graphs whose nodes or edges it actually wrote (the only ones stamped) - and none of the four directions had a test: whether a read stamps, whether a batch that wrote nothing stamps, whether a graph only resolved (never written) stamps, whether a graph really written stamps. The new case rewinds two graph clocks to 1000 and 2000, then checks: `ListL3`, `GetL3` and `QueryL3Subgraph` leave both where they were; a `Skip` batch resolves both domains, skips all four nodes and still names both graphs, yet moves neither clock; the batch that rewrites two nodes of one graph advances that clock and leaves the other exactly at 2000; renaming a graph counts as a change and stamps it. The clocks are read off the records, so an advance is never a listing that stamped on the side. **Four negative proofs, each red on its own assertion**: letting `ListL3` stamp (reads advanced the clocks); counting `Skip` as a change (a batch that wrote nothing advanced the clocks); stamping the resolved set instead of the written set (the same message); never marking a graph changed (the graph this batch wrote kept its old clock). The facade comment said the clock moves on a write and stays when a batch changes nothing, and omitted reads - now stated. No production code change; surface and disk format untouched.<br>51. **Node decay re-bases the very clock it reads, and that design now has an arithmetic invariant** (`internal/cap/engram/decay_compose_test.go` + the `SceneNodeView` comment in `api/types.go` + one assertion given teeth in `internal/repo/l1sync_test.go`): an L1 node has no separate decay clock - fading is measured from `UpdatedAt`, and the pass that applies it re-bases that field. The two only agree if decay composes: two one-hour passes have to land on the importance a single two-hour pass reaches. It does (difference under 1e-12), and nothing pinned it: measuring `dt` from `CreatedAt` instead fades the same node to 0.9704 across two passes against 0.9802 across one - the first hour counted twice, which is exactly why the re-base exists. The new case pins three facts: the clock lands on the pass's own moment; two one-hour passes equal one two-hour pass; and a node whose clock another writer moved inside the interval is not faded for all of it. **Two negative proofs, each red on its own assertion**: deleting the re-base line, and switching `dt` to `CreatedAt`. The three writers of that clock (`l1layer_sync` moves it only when the scene's topic set changed, `decay` re-bases it every pass, `BackfillL1Emotions` only when the values really moved) were not stated anywhere the host reads: `SceneNodeView.updated_at` just said milliseconds, so a host would order memories by it as if it meant last content change. The comment now says what it answers: the clock forgetting measures from - when this memory last mattered, not when it was written. Both halves of the sync rule are pinned now: the no-op assertion stops skipping itself when the record is gone (it read err == nil && UpdatedAt != first, so a vanished node passed silently - a mutation that deletes the node on the no-op path is now reported), and the other half, that a changed topic set has to write a fresh clock, is tested by parking the clock on a sentinel value of 1 and requiring it to move (deleting the re-stamp step trips it). The two package docs (`internal/cap/agent.md`, `internal/repo/agent.md`) gained who reads this clock and who moves it. No production code change; surface and disk format untouched.<br>52. **Keyword extraction stopped reporting "the model cannot answer in JSON" for what is a ceiling** (`internal/cap/llmops/keywords.go`, the only production change this round): the attempt ladder is three widening output budgets plus one format-constrained retry, and a reasoning model most often fails by spending every budget on thinking - each reply arriving with finish_reason=length. The last step used to rewrite that truncation as a format failure whose text told the host to check the model's structured-output capability. Both shapes carry ErrLLM, so the message is the only channel the host has to choose between raising MaxOutputTokens and changing model; the old text sent it to debug an endpoint that never refused anything. The ceiling error now passes through as itself (it names max_tokens=N and keeps the ErrTruncated cause), and the chunked path adds which chunk of how many while keeping that cause. Same doctrine the repo applied twice before, for consolidation and distill misattributing a failed format retry. No new public error code, no method-count or format change. Evidence: TestKeywordExtractionKeepsATruncationAsTheCeilingItIs (the ladder runs its four calls, the truncation survives, the text no longer says no parseable JSON) and TestChunkedExtractionNamesTheChunkAndKeepsTheCause (5000 runes split into three chunks: chunk 0 of 3, stopping at the first failure). Four negative proofs, each red on its own assertion: masking the truncation again, dropping the cause when wrapping, dropping the chunk number, and removing the wrapper.<br>53. **A host can keep exactly one identifier per domain: the id is readable and it opens the door back** (`Session.AgentID` + `DB.Agent(llm, id)`, two additions to the public surface, ruled by the user): v1.6.4 retired the domain id because the name was to be the only handle, yet a host still stores one identifier per memory and the name has to be re-typed at three entries, where one typo quietly creates a second sub-domain. `AgentID()` renders a domain's 16-hex id (the primary's being the implicit zero value) and `DB.Agent(llm, id)` returns that same domain, re-pointed at the endpoint passed in. It creates nothing: an id this file never registered is refused with ErrAgentNotFound, and a string that is not hex is refused with ErrInvalidQuery before any domain is touched, so a mistyped or invented id cannot open an empty memory over a real one. The id stays opaque, library-issued and never host-constructed - what was given up is only the formal gain of keeping it off the surface. The test pins the shapes: three distinct 16-hex ids, the same id after reopening still reaching worker's own profile, a second id landing on a different domain, the primary's id reaching the primary, and each refusal carrying its own code. Taking a domain by id also re-points it, so a live domain moves to the new transport (`TestAgentByIDMovesTheLiveDomainToTheNewEndpoint`); four negative proofs each land on their own assertion - dropping admission, dropping the parse, always answering the primary, and dropping the endpoint swap. **Surface moves from 25 + 7 to 26 + 8**: the pinned lists, the facade-doc method count (34), both guides' entry sections and the §9 cheat sheet, AGENTS and the internal/agent.md 'two doors' item all corrected in this batch; the decision archive's rejected alternative 'keep an id-based escape hatch' is rewritten as adopted on 2026-09-23 with the original reason and why it no longer holds. Disk format 0x0012 unchanged - the domain id has always been in every record frame - and the change is purely additive for hosts.<br>54. **A domain id is scoped to its file - the three-repo e2e walk found that on first use** (new `TestAnAgentIDAddressesADomainInsideOneFile`; one paragraph added to the facade comments, both guides and AGENTS): deployed as one file per agent, every file's primary is the implicit zero domain, so two files' primaries report the same 16 zeros. A host that keyed a global table on the id - the reading item 53 invites - would fold two agents' memories together. The text now says what holds: an id names exactly one domain **within one file** (which is what makes `DB.Agent` unambiguous), and across files the key is the path. The test measures both halves - the same zero id in two files reads back each file's own profile - and pins that inside a file the primary's id and a sub-agent's id differ, so a per-file map on id has no collisions. The guides' 'keep one identifier per memory' is qualified to 'within one file'. The e2e program grew from 14 checks to 16: reopening this file's primary by id returns the same topics, the two kinds of id inside a file differ, and an invented id is refused at the door. No production code changed - documentation comments and one test - and the surface and disk format are untouched.<br>55. **A discovery door: `DB.Agents()` lists which domains the file holds** (ruled by the user; `internal/agents.go` + `api/open.go`, surface 26 + 9): the id door opened last round could only be walked one way - know an id, get the domain - while a host with an inherited `.meh` or a lost roster could not ask what is inside, and guessing a name is worse than asking: `SubAgent` with a wrong name quietly builds a second empty domain beside the real one. `Agents()` returns `[]AgentInfo{ id, name, primary }`: the primary leads (its id is those 16 zeros), the rest follow in id order, so the same file answers byte-for-byte the same - that is the 13th pure read in `surface_order_test.go`, encoded eight times. It reads the registry records from disk rather than the in-memory table, because this list's job is completeness: a tenant key that resolves to no name stops the listing with that cause instead of returning a shorter, wrong answer. The host shape is list, pick, open back, and the test walks both ends - every listed id opens the domain it names, `GetL0` confirms the name, and the roster is identical after reopening the file. Three negative proofs each land on their own assertion: dropping the sort, dropping the primary, swallowing the unreadable key. The file-wide L3 pool is not a domain and carries no registry record, so it cannot appear; the new exported type is added to §9 of both guides and the facade comments, and AGENTS' 'no tenant registry' line now reads 'no cross-process multi-tenant registry - `DB.Agents` lists this file's domains'. Disk format unchanged, purely additive.<br>56. **'Return a plan-style context' now has a measured price, written into both guides** (new `BenchmarkEnginePlanContext` in `test/benchmark_engine_test.go`, no quota and no network): a host pushes this turn's plan into the prompt every loop, and nobody had measured what 'returns a compressed context' actually costs. Two reads are the whole price - `PlanState()` for the tree and one `SearchL4{TopicID, Kind: event}` for the step-bound events, where `NodeSeq` on each row is the attribution. The corpus is the shape you described: a 21-step tree (a root, five children, three leaves under each) with an event of its own per step. On an Apple M2 against the offline stub (-benchtime=100x, two matching runs) that pair is about 97-99 microseconds and one answer is 8 268 bytes of JSON, roughly 0.4 KB per step counted on the wire format rather than the text a host renders. The paragraph also states the truth about 'compression': the engine truncates nothing and returns the whole forest - the only compression is semantic, a done parent's summary folding once every direct child is terminal (TestPlanRollupWaitsForEveryChild) and a step reading as itself plus its subtree (TestStepReadCoversItsSubtree) - so the token budget stays the host's, and no budget-shaped read is added before a host is shown needing one. The corpus reports its own shape (steps, events and the tree's total all asserted) so an empty tree cannot be timed and reported as cheap; the negative is removing the per-step appends, which trips the assertion on the spot. No production code changed; surface and disk format untouched.<br>57. **The graph copy a host must perform now runs, and its one type conversion is gone** (new `test/api_interface_graph_copy_test.go` + `api.HypergraphNode.SourceRef` from `*string` to `string`): a worker with its own `.meh` starts with an empty L3 pool because the pool is per file, so copying the project graph is a host's job - and nobody had walked it. It turns out to need only the public surface: `GetL3` returns nodes with titles and hyperedges as member-id sets, so a host writes one `L3ImportItem` per node and hangs each edge under its lowest member as that item's `Related` (a hyperedge is unordered, so the anchor choice loses nothing); importing under the same `Domain` puts the copy on the same id in the other file, since a graph's id derives from its label. The test compares every node field and every edge by kind plus sorted titles, and requires a replay of the same copy to stay at three edges. The only conversion on that path was `source_ref`: a string on the write side, a pointer on the read side - yet the library never stores an empty reference (merge keeps the old value, overwrite clears to nil), so both spellings mean the same set: absent, or a non-empty reference. The read side becomes a string; under `omitempty` the encoded JSON is byte-for-byte what it was (measured: a nil pointer and an empty string both drop the key, and a non-empty one emits the same token), and a host no longer dereferences to write a read node back. `TopicSlot.parent_id` keeps its pointer, because it answers a real question (topical or folded away) with no write side to pair with. **Breaking for Go hosts** at any call site that wrote `*n.SourceRef`. Two negative proofs, each red on its own assertion: a copy that drops `Related` reports zero edges, one that drops `Keywords` reports a changed node. The L3 section of both guides gained the copy recipe and AGENTS its offline count (29 to 30). Disk format unchanged.<br>58. **The host's half of the profile is written whole, not merged - the rule lived only in the code** (new `TestUpdateL0WritesTheHostHalfWhole`, the `ProfileInput` doc, and a paragraph in both guides): `ProfileInput` says the non-`Name` fields may be left empty and never said what empty does. Measured: it replaces. Naming one preference leaves only that one, omitting `Role` clears it, and a nil table means the same as an empty one; only the distilled pair and `AgentType` are inherited (TestUpdateL0KeepsDistilledHalf). The guide's line that Name, Role and Preferences have the host as their only writer stated ownership, not merge semantics, so a host building a 'set one preference' tool silently deletes the rest - exactly the failure item 1 of the brief is about. Per-key merging was rejected for a reason worth stating: it leaves a preference with no way to be deleted, so the posture is GetL0, edit the map, write it back, and the new case pins the replacement and that path. **Negative proof**: letting the write inherit an empty Role and merge the tables trips it on the spot with `Preferences survived the edit ... want exactly the one this write named`. No production code changed - assertions and documentation only; surface, encoding and disk format untouched.<br>59. **A sentence two rounds of changes had already voided is gone** (both guides' error-code paragraph, the internal code comment, and the two new doors in the runnable skeleton): `Agent(llm, id)` *is* the place a host hands back an agent id, and it answers 3002 for an id the registry never held - adding the door last round updated the entry section and the cheat sheet but left this farther-out claim saying the opposite. The §11 skeleton now lists the roster and opens a domain back by id (`llm` lifted out of the `api.Open` call into a variable), which is the artifact a host actually copies: `make check-guides` only proves it compiles, `TestGuideSkeleton` proves it runs, and both are green now. Sweep: grep both guides, AGENTS, internal/agent.md and the facade for every 'ids never cross the boundary' or 'no public call produces that code' sentence - the only survivor is the lock-order note about `contextFor` checking the registry, which remains true. Documentation and comments only; no shape or production change.<br>60. **The read side now measures time bounds with the same ruler as the write side** (`wrongScale` in `internal/content` decides the two bands once, `CheckQueryBound` guards a query bound, and `SearchL4` calls it): `CreatedAt` has long refused the seconds (1e9-1e11) and microsecond (>1e14) scales, but `SearchL4{Start, End}` did not - while those bounds are compared against each record's millisecond stamp. A wrong-scale bound is never the window it names: a seconds `Start` sits below every stamp and lets the whole library through (measured: that query answered 4 rows), while a seconds `End` excludes every record and invites the host to read an empty result as 'nothing was recorded then'. The same doctrine already applied twice here (enum filters refuse undefined values; the write boundary refuses wrong units); this is the third. 0 still means 'this bound is unset', which is why the write check and the bound check are two functions over one set of constants. The new case pins five bounds, including the positive control with correct milliseconds and a small relative value still allowed. **Negative proof**: drop the two calls and the seconds `Start` answers with rows instead of refusing. L4Query's field comments, both guides' L4 section and AGENTS' millisecond entry updated in the same batch; surface and disk format unchanged.<br>61. **How far `Dream(ctx, sceneID)` reaches is measured for the first time** (new `test/api_interface_dream_scope_test.go`, two cases, plus a paragraph in the facade doc and both guides): the library has long refused a scene id it cannot find rather than reporting a clean no-op, and has long run both retention prunes on every pass - but the page a host reads said only "pass a scene id to consolidate one scene". Two surprises followed from that: a model echoing a deleted scene id gets `ErrNotFound` (not an empty report), and a scoped pass's report still counts pruning done in scenes it never named. The first is now pinned (delete a scene, `Dream(id)` fails, `Dream("")` on the same domain still succeeds); the second with an unfriendly-looking shape - two scenes of two turns, `DreamCompressMinTopics=2`, one day-old record planted in the scene the call does NOT name (the write boundary allows it, no sleeping needed) - after `Dream(scoped)` the named scene carries the only fused group (`ConsolidatedScenes=1`, `L2TopicsCompressed=2`, one depth-1 row with children), the other has none, and its aged record is gone with `l4_prune` reported `ok`. One criterion I nearly got wrong and fixed before committing: fusion cannot be read off a row count, because the scene read lists a group together with the turns it swallowed (three rows either way) - it is the `Depth == 1 && ChildCount > 0` row that says a group exists. **Two negative proofs**, each red on its own assertion: letting the scene lookup treat an unreadable scene as an empty set (the call then answers code 0 with an empty report), and making the prunes honour the scope (the aged record survives). Surface and disk format unchanged.<br>62. **Every host-facing shape now spells its keys the same way, and a gate keeps it so** (json tags added to `core.TurnEnd` / `core.ScenePatch`; new gate `api/surface_keys_test.go`): every response DTO and every query input already carried snake_case keys (`scene_id`, `content_type`, `node_seq`, `graph_id`...), except the two shapes a host hands in on every turn - `TurnEnd` and `ScenePatch`, which had no tags at all. The cost was not looks but a second naming scheme: a host generating a tool schema, echoing a call into the event track, or diffing two reads found `CreatedAt` standing next to `created_at` - exactly the cross-repo translation the brief asks to remove. Go field names are untouched, neither type is persisted or encoded on any path, so disk format 0x0012 is unchanged. The new gate starts at every exported method of `Session`/`DB` and walks each reachable exported field, requiring a json tag, a snake_case key, and that any hidden field sits in the allowance table with its reason present as a marker in both guides. That last branch earned its keep at once: `MBTI.Type` is derived on every read and never stored - the axes are the only fact on disk - so 'readable in Go, absent from JSON' became a written contract instead of a silence, and the guide points a host at `ProfileBrief`, whose `mbti: ESFP` line is already rendered. Four negative proofs, each red on its own branch: stripping TurnEnd's tags, adding an untagged field, adding a hidden field with no recorded reason, and deleting the marker from the guides. The earlier shape gates checked that lists were non-nil and no null was encoded; key spelling had never been checked.<br>63. **The import vocabulary moved from `Skip/Merge/Overwrite` to `skip/merge/overwrite`, and the whole set of vocabularies is now pinned** (`internal/repo/core/model_dto.go`, new gate `api/surface_enums_test.go`, a new §7.6 table in both guides): every string value a host can see already followed one spelling - plan statuses `in_progress`/`done`/`failed` - except `L3ImportMode`, whose three values were PascalCase. That is precisely the value a model is most likely to fill in on the tool path, and following the surface's own instinct (`overwrite`) got refused, so the host needed a one-off mapping for that one argument. With lowercase values `L3ImportMode(wordFromModel)` just works; the Go constant names are untouched and the type is never persisted or encoded, so disk format and existing files are unaffected. The refusal text is now built from the constants - it used to read "mode must be Skip, Merge or Overwrite", whose advice would have been refused, and an error a host cannot follow is worse than no error at all - and it names the value it was given. The new gate checks three things: every string enum value is lowercase with underscores; for the three numeric enums the `String()` word, the encoded number and "one past the set is invalid" all match the table; and every word in that table must appear as a **whole word** in both guides (`\b` matching - the `merge` inside `L3ImportMerge` is a Go identifier, not advice to a host; my first version used a substring test and passed for free). **Four negative proofs**: values back to PascalCase, `ContentOther` moved from 255 to 7, `KindUtterance` renamed to dialogue, and one guide word replaced. Honest note: the gate's function initially lacked the `Test` prefix, so it reported "no tests to run" - a green that proved nothing; naming it fixed that, and the negatives are what proved it afterwards.<br>64. **Item 4's "expose it as LLM tools" gets a table worth copying, checked in both directions** (new §7.7 in both guides + the gate `api/surface_tool_map_test.go`): a host binding this surface to a model writes eighteen tool schemas - a name, the keys to ask for, what to do with the answer - and until now that mapping lived only in the host's head, with the key names only just unified (last round's `TurnEnd`/`ScenePatch` tags are what made one table possible at all). The table gives a suggested tool name per task-face method and its keys, and states why the eight admin-face methods are deliberately excluded: a model holding a delete needs the host's approval path, not a schema. The gate runs both ways: forward, each row's method must exist with the width the row claims and its keys are read straight out of the struct tags (adding a field without listing it fails - proven by adding `scratch`, which failed both guides); backward, every exported session method that is not admin-face must have a row (adding `ScratchFace` produced "§7.7 owes it a tool row"). Row count must equal the task-face count, no admin method may appear, and keys must match as whole tokens - not as substrings of identifiers, the fake green from last round. Four negative proofs in all. AGENTS' test inventory gained the gate. No production change; surface and disk format untouched.<br>65. **The tool table now says which two keys stay the host's** (a paragraph in §7.7 of both guides, an assertion in `api/surface_tool_map_test.go`, one sentence in `AppendArchive`'s go doc): two rows carry `created_at`, one also `seq`, and a schema that asks a model for an epoch in milliseconds gets seconds or nothing back - which the write boundary refuses, since the retention window counts milliseconds. The rule now written down: the host stamps the clock even on a model-initiated call, `seq` 0 means "the next free slot", and every other key may come from the model. The facade comment already said CreatedAt is the host's; it now also says what that means when the call is a tool. The gate requires the paragraph and both key names to stay next to the table (negative: deleting the marker in the Chinese guide reported "the tool table no longer says which keys the host fills itself", and restoring it turned green). No behaviour change - documentation plus one assertion.<br>66. **"ProfileBrief is bounded" became a number instead of an adjective** (new `briefWorstCaseRunes`, `TestBriefIsBoundedAndRepeatable`, and the `ProfileBrief` row in both guides rewritten): the guides claimed a bounded digest without saying how large or which preferences survive when there are dozens, so a host planning a prompt budget had nothing to hold. Measured now: `name`/`role`/`personality` capped at 160 runes each (an ellipsis marks a truncated one), the type word on its own line, the **five lowest** preference keys in order (keys 160, values 120), worst case 2010 runes against a 2100 ceiling, and two calls must return byte-identical text. The fixture needed a second pass: the first version gave all 40 keys the same 160-rune prefix, so after truncation the five looked identical and the unsorted mutant escaped; with distinguishable keys the same mutant failed on "the same profile digested twice differently", and a second mutant raising the field cap to 400 reported four unmarked truncations plus the ceiling breach. `internal/cap/agent.md` updated too. No behaviour change - the library already sorted and took five - this states how large and which five, and holds both to it.<br>67. **One scene can no longer answer twice in one turn** (new `test/api_interface_scene_parity_test.go`, one sentence added to §6.5 in both guides): a loop reads the same scene twice - `Search` hands back the surface topics when it opens the turn, `SceneContext("")` returns the transcript flattened to depth ≤ 2 for recall. Both come off the same `ac.L2Meta` cache, but until now nothing asserted that `Search`'s rows *are* the transcript's depth-1 rows: a divergence in order or in the depth filter would show up as two contradictory answers about one scene inside a single turn. The new case pins the same rows, same order, field-for-field equal, and does it **after a consolidation pass**, so the two levels genuinely differ (four settled rounds → one pass sinks two → three surface rows, five transcript rows). It asserts non-decreasing `UserTimestamp` across the surface rows and equal keyword tracks row by row, and carries its own guard that the transcript really is deeper — without that the case would degenerate into comparing one read with itself. **Two negatives, each red on its own assertion**: inverting `SurfaceTopics`' sort produced "row 0 is … from Search and … from SceneContext, so the two reads disagree on order"; loosening its depth filter produced "Search listed 5 topics while the transcript's depth-1 rows are 3". No behaviour change; the offline interface suite went from 33 to 34 cases.<br>68. **The consolidation pass's stage names and order became a closed, published set** (new `test/api_interface_dream_stages_test.go`, a paragraph in §6.4 of both guides): `DreamReport.Stages` is the only way a host sees which step did what and which never ran, yet both guides listed only the struct fields and the four status values - never the nine names - so observing a stage meant reading the source, and renaming one in code would break nothing. The measured order of a full pass is now pinned: `l4_prune → l5_prune → l2_compress → index_rebuild → l1_nodes → l1_hyperedges → l1_rebuild → l1_decay → l0_distill`, with the reason the prunes lead (a domain with nothing to consolidate still sheds expired content and plan nodes), why `index_rebuild` sits before every L1 stage (the rebuilt L2Meta is installed before anything reads it), and that a stage never reached is **absent**, not `skipped` - which is a different answer from a cancelled run. The gate checks both sides: the emitted sequence item by item with statuses inside the published vocabulary, and the nine names present as whole words in both guides behind a `dream-stage-order` marker. **Three negatives**: renaming `l0_distill` to `l0_profile` failed on "stage 8 is ... want 'l0_distill'"; deleting the chain line from the Chinese guide failed on "does not name the stage 'l4_prune'"; and changing only the prose - where the name still appears in the chain - correctly stayed green, which is the distinction the second fixture had to get right. No behaviour change; the offline suite went from 34 to 35.<br>69. **L1 goes from a name in a tool table to a section, with a gate that checks the field table against the shape** (a `### L1 scene associations` / `### L1 场景关联` section in both guides + `TestGuideFieldTablesEnumerateTheShapeExactly`): acceptance item 2 says the host may read L1 while Dream owns its updates, yet the guide jumped from L0 straight to L2 - `ListL1` appeared only in the tool table, the face list and a DTO name row, so a host learned nothing about what a scene node stands for, that `topic_ids` is a last-Dream snapshot, that an edge has no read of its own, that `updated_at` is the decay clock, or that the layer has no write surface at all. Writing it exposed a fabricated field of my own: the row listed three emotion axes copied from a profile, but `SceneNodeView` carries `valence` / `arousal` only - profiles have three axes, nodes two - and no existing gate could catch it (`TestGuideIdentifiersResolve` watches capitalised backticks, and the snake_case walk only checks spelling, while `dominance` is a real key on another shape). The new gate compares per shape: the keys in a marked table's first column must equal the JSON keys that shape reflects, so naming a key that never arrives and leaving a key unlisted both fail. Negative proof run against the real guide: adding `dominance` back reports `the table for SceneNodeView ... names keys the shape does not emit: [dominance]`, reverting goes green; a fixture covers the omission direction too. No behaviour, surface or format change.|
| v1.6.5 | 2026-09-22 | The MCP surface is retired: the Go module is the only way in |1. **`cmd/memhop-mcp` deleted whole** (25 tools, multi-tenant HTTP over SSE and streamable-http, the tenant registry and the `--tenants` list that bounded how many domains one file may grow). 2. **Deps 4 → 3**: it was the only reader of `modelcontextprotocol/go-sdk`, so `go mod tidy` also drops the 7 indirect deps it carried. 3. **Go surface unchanged**: `Session` 25 + `DB` 7, with `api.CodeOf` kept as the host's seam for reading an error code. 4. `make build-mcp`/`test-mcp` are gone and the CI/hook package lists lose `cmd`. 5. Every "Go-only" phrasing becomes a constraint on the caller (`CompactTo`'s argument is an arbitrary write path; the host pins the destination), and one attribution is fixed: `SceneContextTopic.messages` is empty in JSON because of `omitempty`, not because a tool chose it. Disk format `0x0012` unchanged|
| v1.6.4 | 2026-09-11 | The surface is rebuilt around `Open`: domains come back as handles, and an id never crosses the boundary; the package audit that followed put each method where it belongs and deleted what nothing read |1. **`Open(path, llm, defaults, profile)` → `*api.DB`** decides by what the file holds: file there + primary profile there → the file's own profile wins and the argument is ignored; file there without one → seeded from the argument, or refused without it; no file → created and seeded, or refused **with nothing left behind**. Both refusals happen before the filesystem is touched. `OpenMulti`, `MultiAgentDB` and `MemHopConfig` are gone<br>2. **Domains are handles, not ids**: `Primary()` returns the domain the file was opened on, `SubAgent(llm, profile)` creates or returns one addressed by `profile.Name`, optionally on its own LLM endpoint. `CreateAgent` / `ListAgents` / `Session(hexID)` / `DefaultAgentID` are gone — a host never holds or echoes an agent id<br>3. **L0 gains `AgentType`** (0 = primary, 1 = sub): stamped at creation and inherited on every host write, so editing a profile cannot move a domain between the two<br>4. **L1 opens a read face**: `ListL1() → []SceneNodeView`, stable order, hex ids. Dream remains the only writer, so there is no L1 write call; `EdgeIDs` has no read of its own — two nodes sharing one are a pair Dream judged related<br>5. **L2 gains `RenameTopic(topicID, name)`**: the name is the host's, never derived, so none of the three paths that rewrite a topic record touches it — building a fused parent, sinking members under it, and replaying the same turn key (`Update` settling a turn twice rewrites the engine-owned half and carries the stored name forward); empty is refused (it means "not named yet"), an unknown topic is `ErrNotFound`, and the new name shows up on `Search` / `SceneContext` immediately<br>6. **Retired**: the whole capability surface (`Crystallize`, `ParseCapabilityPackage` / `ValidateCapabilityCard`, the `internal/cap/capability` package), `ListTrajectorySessions` (read the event track with `SearchL4{TopicID, Kind:event}`), `DeleteL3Nodes` (L3 deletion has one granularity left: the whole graph) and `DeleteAgent` with its whole-domain delete chain. Surface 27 + 8 → **26 + 6**<br>7. **Format `0x0011` → `0x0012`**: `0x0011` and older are refused at Open with no migration — their profiles carry no `agent_type`, so every domain in them decodes as the primary against a rule that a file has exactly one<br>8. **MCP stays at 24 tools**: `memhop_crystallize` and `memhop_trajectory_sessions` out, `memhop_l1_nodes` and `memhop_topic_rename` in. Each `/mcp/<tenant>` reaches its own sub-agent domain by name, created on first access; the shared file is opened at startup, so an unusable configuration fails the process instead of the first request. The tool descriptions are re-aligned with what the code returns: `memhop_dream` now hands the partial report back alongside the error (the shared `handle` wrapper used to drop it, while the facade's own contract promises exactly that record of how far the pipeline got), `memhop_profile_update` declares `name` required and says the four fields are a whole write rather than a patch (an all-`omitempty` shape reads like a patch, and an omitted field was being cleared), `memhop_scene_list` no longer promises a topic count, `memhop_scene_topics` describes the flattened depth-≤-2 list (a fused parent plus the turns it swallowed), and `memhop_search` no longer claims a topic carries L4 archive ids.<br>9. **`DefaultMemHopDefaults` is a value, not a pointer** — copy it to tune one open, instead of every caller sharing one mutable global<br>10. **Read errors keep their classification at the read site (two sites)**: `repo.GetProfileL0` no longer rewrites everything as `ErrNotFound` — an unreadable record reports `ErrIO`, an undecodable one `ErrDeserialization`. Three upstream branches (when `GetL0` answers with an empty profile, when `UpdateL0` declines to inherit the distilled half, when Dream skips a rewrite) had been unreachable, and one of them documented itself by that branch. What is fixed: one transient read failure could let `UpdateL0` wipe the domain's `EmotionState`/`MBTI`/identity. Pinned by `TestUnreadableProfileIsNotAbsentProfile`. The same classification now holds at the compression sink: a group member the listing names but the payload will not decode was skipped as if it had gone away, leaving the fused parent and that member's own originals both in the scene — every rewrite is held until one last batch and an unreadable member stops the whole group so consolidation rolls it back (`TestCompressTopicsL2RefusesUnreadableMember`)<br>11. **Dream installs its rebuilt cache before the L1 stages**: the whole-table L2Meta rebuild goes back on the domain context as soon as it is computed, since L1 writes only L1 records and cannot invalidate it. The reverse gap is now written down: this package's compression rewrites topic depth with **no incremental mirror step**, so that rebuild is its only reconciliation point (`TestStructureStagesKeepsRebuildAcrossL1Failure`)<br>12. **One prologue and one parser for the turn key**: `db.lockSession` (domain lock → parse turn key → unlock on a refused parse) existed with zero callers while every L4/L5 entry hand-wrote the same lines; `AppendArchive`/`PlanNodeAdd`/`PlanNodeUpdate`/`PlanState`/`Update` now share it. 'Parse a topic key and refuse the reserved zero' was written twice (`turn` and `content`, differing only in message) and is folded into `content.ParseTopicID`, which also closes the two entries (`RenameTopic`, `DeleteTopic`) that parsed without refusing zero<br>13. **One implementation per judgement and per shape**: the three equivalent 'the index names it but the record will not read = `ErrIO`' loops collapse into `repo.ReadArchivesByIDs`, and `scene.ContextTopic` becomes a pure renderer (a small-method package no longer reaches into `core`); the `repo.ArchiveContent` look-alike of `core.ArchiveSlot` is deleted (every record was copied twice on its way through); `CompressTopicsL2` stops returning time bounds nobody read (Dream computes the same pair itself and it is that value which mints the parent id); the L1 emotion backfill moves out of the L0 module and stops calling a read failure 'node not found'<br>14. **Dead code and unread fields deleted**: the hand-maintained record counter inside the engine (the header count is computed from the index, and the read at Open had no consumer), `ListTopicsL2`'s single-topic-by-id mode value (its mode bit is gone entirely by item 22 - listing one scene's topics is the only route left), `MemHopConfig.Validate`, `L2MetaIndex.Len`, L3's `Node.Importance`/`Edge.Weight`/`Edge.Label` (no write path or a constant write, and never in a public DTO), and `llmops.L1Sample` is now an identity alias of the one concrete shape, `core.DistillSample` (the same precedent as `EmotionScore`/`MBTIScore`). **Disk layout and `FormatVersion` do not move**: a JSON key an older file carries that the record no longer has was already skipped at decode<br>15. **Public surface converged** — the breaking half of this round:<br>A) **A topic stores no child list**: `TopicSlot.ChildrenIDs` goes, together with the host-visible `children_ids` key. Nothing in the engine ever walked it — both the subtree closure and the `child_count` a host sees are computed from each topic's own `parent_id` — so it only contributed a pruning step on every delete, one more mirrored cache field, and a chance to disagree with `parent_id`. `scene.PruneParentChild` and `common.RemoveOnce`, which had just lost its only caller, go with it<br>B) **Profile writes take `api.ProfileInput`**: `Open`/`SubAgent`/`UpdateL0` now receive only the four host-owned fields (`Name`/`Role`/`Personality`/`Preferences`). The library's own four moved from 'sent but not believed' to 'nowhere to put them'; the read shape stays whole. `Name` is the one required field at all three entries — a blank one is `ErrInvalidQuery` rather than a stored profile nothing can address (`TestUpdateL0RequiresName`)<br>C) **Two pure-`len` keys deleted**: `PlanNodeView.child_count` (`Children` is right there in the same view) and `SceneContext.topic_count` (it is the number of entries returned). `SceneContextTopic.child_count` **stays** — it is counted over `parent_id`, and a host would have to rescan the whole flattened list<br>D) **A graph's `updated_at` becomes its content clock**: an import advances it once per graph whose contents it actually wrote (one write per graph, not per record); a graph it only read keeps its clock, so a Skip-mode re-import no longer makes a graph look freshly changed<br>16. **Docs made true**: both `PlanTree.DoneCount/TotalCount` descriptions said they count roots while `CountForest` sums every node of every tree (the recursive reading is the one pinned by `TestPlanStateForestMultipleRoots`); the L5 'shared prologue' documented a function with zero callers, and 'content has a single write entry' was broken by the consolidation summary — both rewritten to what runs. The two host guides were their own finding: they carried a 'partly updated' banner while still teaching entry points that no longer exist (`OpenMulti`/`CreateAgent`/`Session(hexID)`/`MultiAgentDB`/`Lock()`/`Crystallize`/`ListTrajectorySessions`/`ParseCapabilityPackage`), §1's hard-contract table and §9's exported-type list were wholly stale and one line still named the format version `0x0011` — both guides are rewritten to the current surface, and the banner now states what is there. 'Each scene keeps at most 20 depth-1 topics after compression' has no constant behind it, so it reads as threshold-triggered convergence; `content.Append`'s claim to be 'the only path that writes a record into a topic' is pulled back inside its own package (the two-entry fact stays in `internal/agent.md`); and `llm`'s two comments named a `Config` type that does not exist while undercounting the fields it read, with three dead pointers in tests and `internal/agent.md` removed alongside.<br>17. **Breaking for consumers**: entry point, handle types, method set and format version all move at once, plus the three JSON keys of item 15, the `ProfileInput` swap and `UpdateL0` now refusing a blank name (it used to store a nameless profile); the host meowagent adapts in its own follow-up round.<br>18. **Five read-side fixes from the review round**: the three L3 listings handed back whatever order the hash-map scan produced, so one host could see one graph in two orders and `QueryL3Nodes`' `Limit` capped an arbitrary subset — nodes, edges and graph slots are now sorted by id and `Limit` is the first N of that order (`TestL3ReadsAreOrderStable`); `QueryL3Subgraph` skipped a node the walk reached but could not read and returned a smaller graph, while that node is one an edge named, so the read failure is now reported (`TestQueryL3SubgraphReportsUnreadableNode`); and `SearchL4`'s topic filter parsed the hex key without refusing the reserved all-zero one, answering a query that holds no turn key with an empty list that looks like a turn that said nothing (`TestSearchL4RefusesReservedZeroTopic`). Two host-facing listings answered a record they could not decode by returning one fewer entry — `ListL1`'s scene-node listing, and `SearchL4`'s unindexed domain-wide route, which had been running against the promise its own `repo.QueryArchivesL4` doc comment makes — so a short list no longer stands in for a damaged record (`TestListL1ReportsUnreadableNode`, `TestQueryArchivesL4ScanReportsUnreadableRecord`) <br>19. **More memory-quality fixes from the review round** (the question asked was whether a turn's memory survives, not whether the call works): one scene's applied merge groups are now member-disjoint — two adjacent threads can name the same turn, and applying both sank it twice, so the first group's summary held a child that no longer answered to it while that turn fell below the deepest read; a later overlapping group now counts as proposed-but-not-applied (`TestApplyGroupsRejectsOverlappingGroups`); and a group whose fused-parent id is already taken is refused the same way — that id comes from the group's timestamp bounds, so member-disjoint groups can still collide on it (`TestApplyGroupsRefusesCollidingParentID`). Re-settling a turn no longer pulls a sunk turn back to the surface: `depth` and `parent_id` are carried over with the host's name, and a settle refuses rather than guessing a position when the stored record will not read (`TestCreateTurnTopicL2ReplayKeepsSunkPosition`). The decay cascade no longer reports an unreadable edge as pruned — a node cannot keep pointing at an edge nobody trimmed (`TestRemoveNodeFromEdgeReportsUnreadableEdge`) — and the L1 rebuild no longer deletes a node whose depth-3 topic merely failed to read (`TestRebuildFromL2StopsOnUnreadableDeepTopic`) — and the topic listing breaks its last tie on record id, so one scene answers in one order (`TestListTopicsL2BreaksTiesOnID`); a decode failure now reports itself as `ErrDeserialization` rather than a bare json error that every caller scored as code 0, and the replay's refusal has its own test (`TestCreateTurnTopicL2RefusesUndecodableRecord`). Dead code went with it: the index's second copy of the `core.IterAll` scan, two `L2Meta == nil` branches that can never hold, a second empty-name check no caller reaches, one unused L3 test helper; DetachGraph no longer decodes a matching scene twice over, and `cap`'s discipline item claimed the layer never reads a layer, which three of its four packages contradict. An enumeration that decides deletions must read back whole: the same line, drawn across the sites that still crossed it the wrong way. The L1 sync answered an unreadable scene node with a freshly built one written over it, resetting importance, emotion and `created_at` and emptying the edge list its hyperedges still name, against the promise in its own comment; the topic closure, the scene batch delete and the scene merge each skipped a member they could not read and still reported a completed cascade, which leaves a live topic whose scene is gone and a child alive under a deleted parent. `core.CollectAllStrict` is the strict twin of the tolerant scan and `MergeScenesL2` returns an error where it returned a bool; `DeleteL2` and its two mode constants are gone, replaced by `repo.DeleteL2Records` and `DeletePlanNodesByIDs`, which tombstone given ids and read nothing — enumerating and deleting are separate calls now, so a cascade runs every enumeration before its first tombstone (one `DeleteScene` used to scan the topic bucket three times, twice after it had deleted) and a refusal leaves the disk as it was, while a consolidation rollback undoes its own id instead of being blocked by the very record that caused it. The plan retention sweep likewise skips a cycle instead of sweeping a tree whose in-flight step it could not see — the exemption is computed over the whole tree — while the domain's cache keeps rebuilding from what survives, because a mirror missing a node looks exactly like one the retention window reclaimed (`TestSyncL1NodesFromL2KeepsANodeItCannotRead`, `TestSyncL1NodesFromL2StopsOnUnreadableTopic`, `TestTopicClosureL2RefusesUnreadableChild`, `TestTopicIDsBySceneL2RefusesUnreadableTopic`, `TestDeleteSceneRefusesWhileThePlanBucketIsUnreadable`, `TestMergeScenesL2RefusesUnreadableTopic`, `TestPlanNodeScansReportAnUnreadableNode`, `TestPrunePlanStageSkipsWhenTheTreeIsIncomplete`, `TestApplyGroupsRollsBackTheGroupWhenASinkRefuses`). Four sites in that family went the same way: an unreadable co-occurrence edge was answered by building a fresh one, restarting the clock decay reads and skipping the guard that keeps a weaker similarity from resurrecting an edge decayed away from (`TestBuildHyperedgesReportsUnreadableEdge`); a subgraph start node that would not read back was reported as 'no such node' (`TestQueryL3SubgraphReportsUnreadableStartNode`); `DeleteGraphL3`, the family's last `bool` site, skipped a member it could not read and deleted the graph around it (`TestDeleteL3RefusesUnreadableNode`); and the tenant registry dropped records whose name would not resolve, so `SubAgent` could hand out a second, empty domain under a name a real domain already held (`TestSubAgentRefusedWhileATenantKeyWillNotResolve`) - creating a tenant is refused while a key is unresolved, opening the file and every resolved name are not. Two topic writers stopped flattening their cause into a bool (`CreateTurnTopicL2`, `CreateFusedTopicL2`): a caller facing `false` could only invent `NewError(ErrIO, "...", nil)`, so 'the stored record will not read back' and 'the write failed' reached the host as one sentence with no code of its own. And a merge group whose members the engine cannot all see is now refused whole, instead of keying a parent by the bounds of the ones it happened to recognise and fusing a summary of two turns over the single child it could see (`TestApplyGroupsRefusesAGroupItCannotSeeWhole`).<br>20. **The read path's remaining orders and refusals were made honest**: a `SearchL4` that spans turns was sorted by `Seq` — a slot number inside one turn — so `Limit` kept the turn with the most slots instead of the newest content, and records tied on `Seq` came back in scan order, so one query could answer with a different subset twice. One topic still reads in slot order; anything wider now orders by the record's own timestamp and breaks a tie on its id (`TestDomainWideL4ReadOrdersByTimeAndKeepsNewest`). A `Dream` cancelled after its compression had landed skipped the L2Meta rebuild entirely, leaving the scene listing turns that pass had already swallowed until the cache was dropped or the file reopened — the cancellation checkpoint moved below the install (`TestCancelledDreamReconcilesTheReadPath`). The graph import batch seeded its label-to-graph map from a scan that stepped over an undecodable slot, so importing the label such a slot held answered "no graph here" and wrote a second graph under it, splitting the domain's nodes across two ids; the seeding and the rename check both read the strict scan now, and a strict refusal names the record it could not read, the engine having no read face that shows damage (`TestImportL3RefusesUnreadableGraphSlot`). The topic listing kept its mirror route and lost the unreachable record-scan fallback along with an error it could never produce, which took the tolerant `CollectAllTopics` with it; and a batch that failed to stamp several graphs now reports those lines in graph-id order rather than map order. A graph's stored origin is retired with it: `HypergraphSlot` carried a `source` whose kind was always "manual" and whose two other fields no path could set, and three of the four `SourceKind` values had no write point at all — so the shape, the enum and the `source` key are gone, and an older file's key is skipped at decode (a breaking change for a host reading that key).The same batch built its per-graph title and edge-key indexes lazily from a listing that stepped over unreadable records, so a Merge import over a node it could not read answered "that title is free" and rewrote the unreadable record in place under its positional id, counting it in CreatedIDs as a new node; the three indexes are now built once, strictly, when the batch is created — which also replaces K graphs × two full-pool scans with two scans. Every error that reaches a host now carries a code: `ReadRecord` translates the frame decoder's bare `io.EOF` into `ErrCorruption` (a bare error takes code 0 through `api.CodeOf`, and 0 is the success code), and Dream's "no scene consolidated" comes back as `ErrLLM`. Cancellation is its own code now (`ErrCancelled`, 5008): each Dream checkpoint and both of the LLM transport's waits — a request already in flight, the backoff before a retry — report that rather than an endpoint failure, and an answer cut off at the output ceiling carries `ErrLLM` with `ErrTruncated` left as the cause the escalation retry reads. `memhop_dream` runs on the MCP request context, so a client that walks away stops the pass at its next checkpoint instead of holding the domain lock through the rest of the pipeline. The order a content read hands back is now described the same way everywhere: `Seq` inside one topic, record time across topics with the id breaking ties, and `Limit` keeping the tail of that order. Five outward texts (facade comment, `L4Query` docs, the MCP search tool description, both integration guides) still promised a flat `Seq` order, and the MCP one paired it with 「keep the newest N」, which contradicts it. Re-naming a domain through `SubAgent` now moves the live domain's transport, not just the table a future rebuild reads: the endpoint override was only ever applied when a context was created, so a reconnecting host kept spending every turn on the endpoint it had replaced (and every `Update` failed once the old key died). Three breaks in the scene chain are closed: a `Search` whose L3 anchor does not resolve no longer leaves an unnameable scene record behind; `DeleteCascade` tombstones the scene and topic records last, so an interrupted pass stays deletable instead of stranding a scene the cache still lists; and a `SearchL4` filter set to an undefined `Kind` or `ContentType` is refused like the write boundary refuses it, instead of answering with an empty list. The LLM layer tightened where memory quality is decided: a distill reply that is valid JSON but answers none of the contract no longer erases the distilled half of the profile and invents a type word; a per-node row naming an id this pass never sampled is dropped in llmops instead of aborting every Dream at the L1 backfill; no output budget may exceed the endpoint configured ceiling, and truncation escalation now spends exactly that headroom; the consolidation prompt states the caller floor rather than a hardcoded 20, subordinate to the rule that different subjects never merge. A merged-away scene's L1 node goes with it, and the decay pass now prunes an edge member this domain no longer holds, so an out-of-band scene delete converges instead of leaving a ghost that keeps pairing with live scenes for days. The idle sweep marks the context it drops inside the same lock hold, so a caller that fetched it just before the reclaim re-fetches rather than writing onto a dropped cache.<br>21. **Two memory-quality inversions and two loss-amplifiers closed** (round 12 of the same audit): the emotional decay factor measured the absolute value of `valence` on a scale whose midpoint is neutral — how *positive* a memory is — so a mildly positive, barely aroused node froze at `lambda~0` (uncollectable for good, since decay is the only path to deletion) while the most painful ones faded at full rate; it now measures distance from neutral, with a ceiling on the protection (`TestEmotionalBoostMeasuresDistanceFromNeutral`). The L1 emotion backfill read the legal value `(0,0)` as "not stamped yet" and rewrote that node every pass, refreshing the clock decay runs on — now compared before writing (`TestBackfillL1EmotionsLeavesASettledNodeAlone`). In the engine a batch whose write fails half-way is cut back to where it began rather than leaving a part-written frame between intact records, and a frame that fails its checksum is stepped over rather than becoming the cut point that deletes everything after it (`TestUndoAppendCutsBackToTheBatchStart`, `TestChecksumFailedFrameKeepsTheRestOfTheLog`); a `Compact` refusal keeps the read's own code and names the record it could not move (`TestCompactRefusalNamesTheRecordItCannotRead`). An MBTI axis answered 0 carries no strength and reads `X`, so four silent axes derive no type word at all. The value ranges of the distilled fields are now stated where a host can read them (`internal` types are not published), `profile.Samples` ranks before it reads any keywords, the profile digest bounds every free-text field it renders, and the stale prose went: a Dream stage that never existed, three copies of a “≤20 topics per scene” bound no code enforces, two identifiers that were never public, two renamed tests. <br>22. **Round 13 of the same audit (scene writes, the event budget, index enumeration, MCP lifecycle)**: creating a scene answered a record it could not read with "there is none" and wrote a fresh one on top of it — that unreadable scene record holds `TurnSeq`, the counter that mints this domain's turn keys, so the overwrite restarted the issuer and the turns that followed were addressed with keys the domain already held; only `ErrNotFound` may create now (`TestCreateSceneL2RefusesAnUnreadableRecord`). A scene lands in one record write instead of two — the anchor is stored with the row, and the write-once `SetSceneL3ID` that performed the second write is deleted with its only caller — so no refused call leaves behind a scene whose id never reached the host. Offering an anchor for a scene that already exists is now refused on its own terms: it used to read the shared graph pool first and hand back a not-found about a record nobody asked to read, inside the caller's domain lock (`TestSearchRefusesAnAnchorOnAnExistingSceneWithoutLookingItUp`). The 4 KiB event budget measures the whole record now — `EventType` had no ceiling, so carrying the bulk in the step name bypassed it (`TestAppendEventPayloadRefused`, `TestAppendArchiveRefusesAndStoresNothing`). Listing one scene's topics walked the domain-wide mirror, and a consolidation retarget took one lock per row; both are single-lock calls now, and the retarget also rewrites the row's own scene id — a listing that names a topic under one scene while the row says another is exactly the seam this round closed (`TestRetargetSceneMovesTheWholeScene`). Three return values with no reader are gone (`content.Append`'s allocated seq, `L4Index.RemoveIDs`' count, `L2MetaIndex.Remove`'s removed row), along with `Iter`/`GetByScene`, which had no production caller. On the MCP side: the registry lock no longer covers `SubAgent`, so one tenant's LLM round-trip cannot queue every other tenant behind it on a path every request walks (`TestRegistryConcurrentFirstAccessServesOneServerPerTenant`); a failed `ListenAndServe` closes the databases on the way out, since `Close` is what persists the checkpoint; and `--transport` is judged once, in `buildHandler`, before the shared file is opened. Six outward texts over-promised and were pulled back: the admin face claiming MCP has a tool for each of its entries (the two memory-correction calls stay Go-only), `tools.go`'s "one tool per public DB method", `os.Root` described as resolving every file operation (it is what notices a symlinked database name; the open itself goes through the joined path), eleven input aliases in `api/types.go` carrying no documentation at all, the `LlmConfig` zero-value defaults (120 s / 8192) which a host could only find in `internal`, and `memhop_trajectory_read`'s `session_id`, which is the scene id `Search` returns and which answers an empty list when guessed wrong.<br>23. **Round 14 of the same audit (the kernel's destruction paths, L3 address occupancy, how an LLM failure gets classified)**: a `Create` refused by the exclusive lock still emptied the file — `O_TRUNC` is executed by `os.OpenFile` itself, before the lock answers whether another instance is reading it, so the loser destroyed the winner's database on its way out; creation now opens `O_RDWR\|O_CREATE` and clearing the record area is the `Truncate(DataStart)` that follows the lock (`TestCreateRefusesAFileAnotherInstanceHolds`). A rotted length field used to cut off every record after it: a frame whose declared length does not fit the file reports `ErrCorruption`, which the Open scan read as the torn tail and truncated from, and the next checkpoint made that permanent — a damaged frame is now stepped over by searching forward for the first offset whose own checksum passes (that frame's declared length cannot be trusted: those four bytes sit inside what its checksum just disbelieved), truncation happens only when nothing after the cursor reads as a record, and every exit that stepped past something logs a WARN (`TestRottedLengthFieldKeepsTheRecordsAfterIt`); the length comparison is unsigned, so a 4 GiB claim can no longer flip sign and slice outside the map on a 32-bit build, and `frameSpanOf` went with the reader that needed it. The three L3 create primitives rewrote whatever record already stood at their derived id and reported success — a graph label can spell out a node address, and the typed reader answers that collision with `ErrNotFound`, which is the only answer that permits writing — so each one now refuses while `Contains` (kind-blind, which is exactly what this gate needs) names a holder (`TestImportL3RefusesADomainNamingANodeAddress`); `L3ImportMode.Valid()` is the single vocabulary, `NewImportBatch` refuses an undefined mode when the batch is built, and the root's duplicate `switch mode` is gone. Two call sites in consolidation and distill reported a cancelled or refused format retry as an off-contract model answer, because what they returned was the *first* reply's parse error; they now carry the retry's own code with both failures kept in the cause (`TestFormatRetryFailureKeepsItsOwnCode`), and the transport's `attempt == len(delays)` branch — the one that made the loop-tail return unreachable — is deleted. Dead code and untrue text cleared with them: `allEntries`, a machine-local measurement written into a comment, and `MemHopConfig` described as a host input (it is the view the assembly layer builds) in `config`, both READMEs and `AGENTS.md`. Two things were recorded rather than changed: `ConsolidationMaxTokens` equals the library's default output ceiling, so a host that never raises `MaxOutputTokens` has no wider rung to escalate to (a `ponytail:` note says so beside the constant), and a turn's transcript carries no aggregate budget — the domain lock is bounded by cancellation, not by a record count.<br>24. **Round 15 of the same audit (an unwritten timestamp unit, a bypass around the public surface, refusals that carried no code, one shape per answer)**: `ArchiveSlot.CreatedAt` and `L4Query.Start/End` never said what unit they take, while the retention window cuts on `UnixMilli()` and the write boundary refused only `<= 0` — a host stamping seconds had that turn's transcript swept as expired at the next Dream, and its time filter never matched anything. The facade and both guides now say milliseconds, and the write boundary refuses the two bands that cannot be milliseconds (seconds 1e9–1e11, microseconds above 1e14) with the consequence named in the refusal (`TestAppendArchiveRefusesATimestampInTheWrongUnit`). `api.Session` embedded its `*internal.Session` as an **exported** field: all 26 methods are declared in the facade, so the embedding promoted nothing, and all it bought was `sess.Session.Search(...)` handing back the `NewTopicID`, `TurnSeq` and `IDHash` that never cross the boundary — the field is unexported now, the shape `api.DB` already had, and a reflection assertion pins that neither handle exposes one (`TestHandlesExposeNoField`). **Breaking** for a call site that wrote `sess.Session.X`. `api.NewError` is exported so a caller refusing on its own terms carries the same codes: six vocabulary refusals in the MCP tools and `memhop_archive_get`'s "no such record" reached clients with no code at all, against a contract that face had already pinned, and all seven now answer `ErrInvalidQuery`/`ErrNotFound`, asserted by their `[1003]`/`[3001]` prefix. `DetachGraph` stopped at the first scene it could not rewrite, leaving half a domain anchored to a graph its caller had already deleted — and the call order is deliberately delete-then-detach, so no retry reaches that pass; it now collects each failure and reports them together. A comment and a test both asserted something that does not exist: the settle gate is documented as refusing a turn a fusion group already sank, while the id such a turn really has is let through on purpose (`CreateTurnTopicL2` carries its depth and parent back, which is what keeps a replay from returning a sunk turn to the surface) — the comment is corrected and the case rebuilt on a real turn-shaped id (`TestUpdateReplayKeepsASunkTurnSunk`). Three copies went to one: `db.GetL0`'s private twin of `turn.ReadProfile`'s "will not read back is not absent" classification, `scene.ResolveForRead` handing back a whole record whose caller reads one field before re-reading it to open the turn, and `scene.OpenTurn`, a one-line forward whose comment was the third copy of one fact. One shape per answer: two `slices.Clone` sites in the facade encoded a list nobody had written as `null` while every neighbouring list encodes as `[]` (`TestMappedListsEncodeAsEmptyNotNull`), and the same defect sat in the three DTOs that reach a host by alias with no mapping — `L3ImportResult.graph_ids`/`errors` were `omitempty` beside two lists that are always present, so a clean batch answered with a missing key instead of `[]`, and `SceneContextTopic.keywords` had no `omitempty` but was fed by `slices.Clone`. `messages` keeps its `omitempty`: `memhop_scene_topics` nils it deliberately to return topic metadata only. Four facade texts were made true (`AppendArchive`'s stored-field list missing `Role`/`ContentType`, `ArchiveSlot.Role` written as a closed three while the same package documents role 3 on reads, `ErrAgentNotFound` listed as a code a host can receive when no public call produces one, and three places calling the topic order "turn order" when its keys are `UserTimestamp`, depth, id). The offline interface face grew from 21 to 24 tests and its "not empty" assertions became equalities: a fusion group's summary text had never been read back anywhere (now twice, with the role 3 and the parent's user slot that only it holds), the distilled profile is pinned to the `ESTP` its four dimensions derive rather than the `ESFP` the mock answers (so an implementation that started trusting the model's word fails), and keyword tracks, L3 node fields, event payloads and both merged turns' originals are matched by id; three injections were added — an off-contract reply costs `Update` and `Dream` their pass and nothing else (no topic record, no summary, originals intact), and a cancelled context answers 5008 rather than the model's 9002. The MCP face pins every tool's `required` list (renaming an argument used to pass the whole suite) and requires each of those names to be a declared property. Noted, not done: `api.ProfileSlot`'s two field types have no public alias, so a host can read those fields but cannot name the type in its own signature — adding two aliases is a surface addition and awaits a decision. <br>25. **Round 16 of the same audit (the fused summary's clock, what "written once" is judged by, two report figures, the tool face's scales and vocabularies)**: a fusion group's summary took the group's last timestamp as its own `CreatedAt`, and the retention window sweeps on `CreatedAt` — consolidating turns already past the 7-day window wrote a summary older than the cutoff, so the next pass deleted the one piece of prose consolidation produces, after the same sweep had taken the originals it folded, leaving a parent topic that points at no text; it now ages from the pass that wrote it while the group's own span stays on the topic record, which is where a reader looks for it (`TestFusedSummaryOutlivesTheTurnsItFolded`). "Has this node been distilled?" was answered from the values, and both ends of that scale are legal readings: `(0,0)` is very-negative-and-calm, indistinguishable from never stamped, so a settled `(0,0)` was replaced by the next pass and the clock node decay is measured from was refreshed with it — the node record gains an `emotion_set` marker (`omitempty`, absent in an older file reads false, format version unmoved) and write-once is judged by it (`TestBackfillL1EmotionsLeavesASettledNodeAlone`). A settle could rewrite another kind's record into a topic: the lenient read answers a foreign-kind hit with `(nil, nil)`, and while the sink, the fused-parent check and the three L3 creates all refuse that answer, the settle read it as "nothing here" — the topic then reads back as a type it never was and the listing of the kind it was stops seeing it (`TestCreateTurnTopicL2RefusesAForeignRecordAtTheTurnAddress`). Two report figures did not count what their names say: `l2_topics_compressed` is the **topics sunk into fused groups** (it was the group count, so one group over two turns reported 1) and `l1_edges_added` is edges created **or re-weighted** this pass; the values were made true rather than the host-visible keys renamed, and both guides now state what each counts. An engine refusal stopped being counted as a model failure: `applyGroups` folded "the model proposed a group this engine cannot apply" and "the write failed" into one number, which the caller reported as `ErrLLM` for "no scene consolidated" — a disk failure sent the host to inspect an endpoint that had never refused it; the two are split by code now, and `Dream`'s nothing-consolidated answer is `ErrLLM` only when it really is the model's side (`TestApplyGroupsRollsBackTheGroupWhenASinkRefuses`). The per-scene LLM fan-out inside the domain lock had no bound — the scene count was the concurrency, so the endpoint received the whole batch at once and the lock was held for as long as the slowest one; it is a `compressFanout = 4` semaphore now. Two swallowed read failures cost memory quality rather than an error: distill sampling ranked a node whose keywords would not read back as a node with none, handing the model a thinner sample to infer personality from, and hyperedge building read the same failure as an empty keyword set — an edge that drops under the similarity floor never comes back on its own; both now propagate anything that is not `ErrNotFound`. A request arriving after shutdown could reopen the shared database: `CloseAll` nils `db` and nil is also "not opened yet", so the revived database was closed by nobody and its checkpoint never written while the process still reported a clean exit — the registry carries a `closed` flag and both `get` and `OpenShared` refuse on it (`TestSSECloseAllPersists`). Two tools handed back scaled signals with no scale stated (`memhop_l1_nodes`, `memhop_profile_get`), where 0 on either is a reading and not "unmeasured": both descriptions now carry the ranges, the neutral point, how the type word is derived, and that a never-distilled domain or node is all zero. Three of the four hand-written vocabularies were unpinned — only `contentTypeNames` was reconciled with the engine, so renaming a kind left the whole smoke suite green — and one test now checks all four in both directions (`TestStatedVocabulariesMatchTheEngine`); role 3 has no exported name on purpose, so that one is pinned to exactly three keys. `ParseAll` dropped `ParseID`'s cause, so a host heard "invalid id" with no way to tell which one; it now carries the offending string. Deletions and one narrowing: the two plan primitives' returned counts had no reader (`WritePlanNode` always answered 1, and `DeletePlanNodesByIDs`' next stop is `CollectPlanNodes` anyway), the L3 edge id had two `Sprintf` formulas where `EdgeKeyL3` is the one, `GetProfileL0`'s unreachable `ErrIO` fallback rewrote the engine's own `ErrClosed`/`ErrCorruption` as "cannot read", `CreateGraphL3` has only in-package callers so `EnsureGraphL3` is the exported create, and `DistillSample.UpdatedAt` had no reader. Withdrawn and half rejected: requiring every one of the seven distill axes to be answered is withdrawn — `{"emotion":{},"mbti":{}}` must parse and two answered MBTI axes must derive `XNFX`, a contract pinned by `llmops_test.go` and `cap/agent.md` item 6 rather than a defect; "the sample-rank age term is dead on the only call path" is half rejected, since depth>2 nodes keep their `UpdatedAt` and the term is live for them. Noted, not done: `cmd/memhop-mcp` runs on zero `MemHopDefaults` instead of `DefaultMemHopDefaults`, and all three consequences are now written down in `docs/mcp/README.md` (no auto-consolidation, a compress floor of 0 which both disables the too-few-topics skip and renders the prompt target as "down toward 0", and no idle-domain reclaim) — switching is a behaviour change rather than a facade one; and `memhop_trajectory_read`'s argument is named `session_id` while it wants the turn topic id `Search` returns, which renaming would break on the wire. Both await a decision. <br>26. **Round 17 of the same audit (a tolerant scan that decides something, and the facade's stale claims)**: the seven listings that do not merely report but decide — L3's four read faces and the three L1 passes that enumerate a domain's nodes — now walk the strict collect and refuse with the record's own code and its id named, because a subgraph adjacency that lost a member is not one less edge but a member the walk can no longer reach, and decay is the only pass that ever removes a node, so one it steps over never fades; the three tolerant twins are deleted and the strict graph-slot collect loses its `Strict` suffix (a rename, both call sites in this batch). The tolerance that stays — the mirrors rebuilt at open — now leaves a trace: `IterAll` logs every dropped record by id, and `BuildL4FromEngine`'s stated reason is corrected, since a torn tail never reaches a scan whose frames already passed CRC at open; what a drop really costs is a Seq `MaxSeq` no longer sees, so the next append to that topic overwrites it. The stale rebuild's removed edges now reach `l1_edges_removed`, an empty profile's `preferences` encodes `{}` instead of `null`, and `Personality`'s two writers (the host's `UpdateL0` and Dream's `MergeDistill`) are now stated as such everywhere instead of one of them being hidden. Ten more false or incomplete statements corrected across `api/open.go`, `DeleteTopic` (an L1 node's `TopicIDs` is the last sync's snapshot, so a deleted topic stays listed until the next Dream), `MergeScenes`, `SceneNodeView.TopicIDs`, `HypergraphEdge.IDHash` (no method takes one) and five MCP tool descriptions — including `memhop_knowledge_import`, which no longer tells clients `content` is required when the batch pre-check refuses only a missing title or domain (pinned by a new assertion on that nested required list). The three items noted in 25 still await a decision.|
| v1.6.3 | 2026-09-10 | Content is L4's only story: a turn's dialogue and events share one layer, L5 keeps only the plan tree, `Update` only distills |1. **Records merged into L4**: `ArchiveSlot` gains `Kind` (`KindUtterance`/`KindEvent`), `Seq`, `EventType` and `NodeSeq`, and an archive id becomes **positional** — `hash("l4:"+topic+":"+seq)` — so rewriting one `(topic, Seq)` overwrites in place. The replay diff that enumerated a topic's superseded archives and tombstoned the rest is gone with the list it needed<br>2. **L6 is plan nodes only**: new `core.PlanNode` on frame value `0x0F` (freed when the capability record layer retired); `TrajectorySlot`, `NodeType*` and `PlanNodeRef` are deleted. That round's format landed on `0x000F` (this version ends on `0x0011`, item 10); `0x000E` and older files are refused at Open with no migration (the intermediate `0x000E` was never tagged) — their events live in a record type that no longer exists, and an older archive names its owner under a key the current record does not carry<br>3. **`AppendArchive(topicID, ArchiveSlot)` is the only content write** and `content.ValidateAppend` the only gate: kind must be defined, an utterance declares role (user/agent/system — the consolidation role is refused) and medium, an event names itself, `NodeSeq` is an event-only field, and over-budget is refused rather than truncated (4 KiB event / 64 KiB utterance). Validation runs strictly before any write, so a refused record leaves nothing behind<br>4. **`Update(sceneID, topicID)` writes no content**: it reads the topic's own utterances, renders them in `Seq` order with speaker labels and distills them once. A turn with no content left is `ErrInvalidQuery` without reaching the LLM; a failed distillation leaves the records the host appended exactly where it put them. Deleted: `TurnUpdate`, `turn.WriteArchives`, `llmops.ExtractTurnKeywords`<br>5. **Surface 26 → 25**: `AppendTrajectory` and `ReadTrajectory` deleted (the read is `SearchL4{TopicID, Kind}` — same rule as the old `GetArchive` removal), `AppendArchive` added, the plan write's event argument takes `ArchiveSlot` (item 10 re-shapes that write surface into per-step calls), `api.TrajectorySlot` retired, `RoleSystem` published and `RoleDream` withdrawn, `TrajectorySessionSummary.Steps` renamed `Events`, `SceneMessage.Seq` exposed so a retention gap is distinguishable from a lost line<br>6. **Retention split, cascade retired**: `index/traj.go` becomes `index/l4.go` with kind-carrying entries, Dream prunes `l4_prune` (content) and `l5_prune` (plan nodes, still exempting a tree in flight) on one shared 7-day `ContentRetention`, and an expired plan no longer deletes a turn's content. Transcript completeness now reads: index names it and the engine cannot return it → `ErrIO`; topic present with empty or gapped content → a legal expired end state<br>7. **MCP unchanged at 24 tools, names move**: `memhop_trajectory_append` → `memhop_archive_append`, `memhop_update` shrinks to `{scene_id, topic_id}`, `memhop_trajectory_read` stays as a convenience tool implemented on `SearchL4{TopicID, Kind:event}`, `memhop_archive_search` gains a `kind` filter<br>8. **Accepted regressions, priced openly**: past 7 days a turn's or a fused topic's text is permanently unreadable and only its keyword track survives (Dream's fusion chain never re-reads L4 originals, so that track was always the long-lived artifact); a fused summary becomes a 7-day intermediate; a host's hot path goes from one call per turn to three<br>9. **Fields and layer numbers converged** (same version, second round): Dream's usage-feedback stage is retired along with the scene's read-side counters (`hit_count`/`last_hit_at`) and `topic_count`, which never actually reached disk — `api.SceneSlot` is now `{scene_id, scene_name, l3_id}`; `ArchiveSlot.ContextID` becomes `TopicID` (field and JSON key); the last two id namespaces drop their layer number (`l1:` → `scene-node:`, `l4:` → `content:`); the plan layer takes the vacated number, so the stack is six layers L0–L5 (`RecL5PlanNode` on the same frame value, `l5_prune`, `internal/l5.go`, `repo/l5layer.go`) and the format is `0x000F`; `L4Query` gains a step-attribution condition (refused without `TopicID`), surface unchanged at 25 + 8 / MCP 24<br>10. **The plan layer re-shaped**: a turn's tree is written **one step at a time** — `PlanCreate(topicID, title) → seq`, `PlanNodeAdd(topicID, parentSeq, title) → seq`, `PlanNodeUpdate(topicID, PlanStep{Seq, Status, Title, Summary})` — replacing v1.6.2's `PlanCommit`, which forced an event with every step and so could not restate a plan at all. A node exists only because a create call made one: an event naming a step the tree does not hold is refused instead of growing a branch, and `PlanNodeAdd` under a parent that is not there answers `ErrNotFound` rather than building a chain to host it — which also closes v1.6.2's documented hole where a typo in a path silently opened a second tree. A step is addressed by a **per-turn ordinal** the library hands out (`1, 2, 3 …`, `ParentSeq` naming the step it hangs under, `0` a root) and the record id follows from it (`hash("plan:"+topic+":"+seq)`); the ordinal is an address inside one turn rather than a record id, so it crosses the facade as a plain number. `SearchL4`'s step condition matches a **subtree** (a split step's work lives on its children), the branch walked over the parent links instead of matched as a string prefix. A node loses `plan_type`; the status vocabulary is `in_progress` / `done` / `failed` — `pending` and `running` are both gone, and a created step *is* `in_progress`, so a status is asked only when a step is restated; a parent's summary folds only once **every** direct child has settled, and re-opening a step clears its `FinishedAt`. There is no step-delete call: a step is abandoned by not creating it in a later turn, and the stale tree goes when the turn falls out of the retention window. Format `0x0011` (`0x0010` and older rejected, no migration); surface 27 + 8 / MCP 24|
| v1.6.2 | 2026-09-07 | Plan-event naming belongs to the host (vocabulary retired) |1. **`planEventTypes` and the `plan.ValidateEvent` wrapper deleted**: a plan-bound event (`nodePath` non-empty) takes `EventType` exactly as a bare turn event does — any non-empty name the host chooses, stored verbatim. The constraint did not earn its keep: the engine never branches on `EventType` (every non-test reference in the repo is that one lookup, `ReadTrajectory` echoing the field, and one `fmt.Fprintf` line in the Crystallize prompt), so refusing writes was its only behavior, and the cost sat entirely on the host — a rejected event name silently thins that turn's trajectory, which is how sandbox-adjudication prompts fell out of the audit log<br>2. **One validation point remains**: `trajectory.ValidateEvent` (non-empty `EventType` + `Timestamp > 0` + payload ≤ 4 KB). `AppendTrajectory`'s plan branch and `PlanCommit` still call it **before** `EnsureNode` / `UpdateNodeLocked`, so a refused write leaves the tree and the event log exactly as they were<br>3. **Delivery surfaces now agree**: MCP's `memhop_trajectory_append` already documented `event_type` as host-chosen, and the plan write surface is not on the MCP tool set at all — this round aligns the Go face with the wording the shipped surface already used; 24 tools unchanged<br>4. **That round added no method and touched no layout**: `event_type` is a JSON string inside the record, so neither tightening nor loosening changes the frame. (The method count and format version moved again in later, unreleased rounds — see the v1.6.3 row). **The change relaxes**, so hosts gain an auditable event without touching call sites<br>5. Tests: `TestPlanEventVocabularyRejectsUnknown` becomes `TestPlanEventNamesAreHostOwned` (a host-named plan event is accepted, its name is not rewritten, and an empty `EventType` is still refused before the node chain is built); three refusal assertions across `api` and `test` now trigger on an empty `EventType`. Decision record: `notes/implemented/simplification/2026-09-07-plan-event-vocabulary-retirement.md`|
| v1.6.1 | 2026-09-06 | Surface re-converged (34→32); builtin manual cards deleted; public face split by audience; L5 record layer retired (directory-as-capability) |1. **`ActivateCapability` deleted**: the repo layer's activate was read-card→set-active→write-back, exactly what `UpdateCapability(id, CapabilityPatch{Status: &active})` already does — one capability, one implementation; activation folds into `UpdateCapability` (and passes the same card validation, so a bare fixture without summary/resources is refused)<br>2. **`PlanReplace` deleted**: `SyncPlanTree(planID, nil)` wipes the tree (every node and bound event removed, planID kept, event Seq restarts at 1); seeding the next task is a single-node sync (an empty status lands pending); the `0000000000000000` rejection is unchanged (ParsePlanID runs before the nil check)<br>3. **MCP 31 → 30 tools**: `memhop_capability_activate` folds into `memhop_capability_update`'s `status` param<br>4. **Builtin manual cards deleted**: the nine in-memory manual cards (`internal.BuiltinCards`, listed but never stored) are gone — per the mainstream capability-face design (Claude Code / OpenClaw skills, MCP), a capability pool holds only LLM-triggerable units, and "how to call the library's own methods" is documentation, not capability: it existed only to be skipped by hosts (meowagent) or to duplicate the MCP tools' descriptions (~28 KB flowing through every full `ListCapabilities`). The pool now holds only host-imported cards, crystallization drafts and `plug/` injections; method usage docs live in `go doc api.Session` / `INTEGRATION_GUIDE.md`. Removed with the mechanism: the listing merge (stored-shadow dedup), the Crystallize fold-back name reservation (`ApplyCandidate` drops its `reserved` predicate), and the `CapabilityOriginBuiltin` constant — **breaking for consumers**: meowagent's skip on `Origin == builtin` becomes dead code and its build breaks until its own adaptation round<br>5. **Public face split by audience (docs/comments only, no structural change)**: the 32 session methods are declared as a runtime/task face (22 — the host-driven loop plus every LLM tool binding) and an assembly/admin face (10 + all 8 DB methods — session boundaries and management channels, never an LLM tool); pinned by the grouped list in `api/surface_public_test.go`, declared on `go doc api.Session` and in `INTEGRATION_GUIDE.md` §8<br>6. Format bumped `0x000B` → `0x000C` (the `0x0F` capability frame type retired with the record layer); files at `0x000B` or older are rejected at Open, no migration; MCP 30 → 24 tools (the six capability tools, including `memhop_capability_get`, went with the record layer); after the tag, meowagent adapts in its own round (call sites: `ActivateCapability`/`PlanReplace` from the folds, `CapabilityOriginBuiltin` from item 4, plus item 7's L5 surface)<br>7. **L5 record layer retired (directory-as-capability)**: the engine no longer stores capability cards — the single source of truth is the host's own `plug/<pkg>/capability.json` directory (the host scans and assembles it; crystallization drafts go to `plug/draft/`; activate = file promotion; changes take effect at restart). Deleted: the five session methods (`ImportCapability`/`UpdateCapability`/`DeleteCapability`/`ListCapabilities`/`RecordCapabilityUsage` — public face 32→27, task 22→20, admin 10→7), eight types (`Capability`/`CapabilityImportResult`/`CapabilityListQuery`/`CapabilityPatch`/`CrystallizeResult`/`CrystallizeDetail` plus `CapabilityStatus`/`CapabilityOrigin` with their constants), the whole `plug/`-injection mechanism (Open-time scan, `FileHash` watermark, re-injection dedup, patch survival) and the core record layer (`RecL5Capability`, its typed reader/writer, `0x0F` frames). Kept as pure capabilities, now exported from `api`: `CapabilityFormatV4`, `ParseCapabilityPackage(data, source)`, `ValidateCapabilityCard(card)` — the v4 format's single source of truth — and `Crystallize(ctx, turnID, existing []CapabilityImport)` turned pure extraction: turn events plus the host's current catalog in, candidates out (`Action` create/reuse/merge, `ReuseID` now names an existing card instead of a hex id, unvalidated candidates pass through — filtering is the host's job); the engine writes nothing. `PromptCard` moved into the capability package and drops the `id:`/`package:`/`usage:` lines no host could reproduce. **Breaking for consumers**: meowagent's five capability call sites and eight types break until its own adaptation round|
| v1.6.0 | 2026-09-04 | No-fallback interfaces; file-wide L3/L5 shared pools; unified capability card + `plug/` auto-injection |The v1.5.0 tag was cut before its API surface was complete — hosts requiring it hit `undefined: api.DefaultAgentID / api.NewPlanID / api.ScenePatch`. This release ships the finished line. 1. **Library-minted ids**: `api.DefaultAgentID` names the implicit domain and `api.NewPlanID(name)` mints plan ids; the four `api/ids.go` Format/Parse bridges are gone<br>2. **Merged surface**: `SetSceneName`+`SetSceneL3ID` → `UpdateScene(id, ScenePatch)`, `PlanAppend` → `AppendTrajectory(key, nodePath, ev)`, `ListScenesByL3` → `ListScenes(l3ID)`; `Lock`/`Unlock`/`Session.Checkpoint`/`IsClosed`/`AgentID`/`DistillL0`/`ListPlans`/`GetArchive`/`GetCapability` deleted (34 + 8 methods)<br>3. **Interfaces refuse instead of falling back**: unparseable LLM keyword output is `ErrLLM` and the turn writes nothing (gse/tokenizer fallback deleted, direct dependencies 5 → 4); a rejected `PlanCommit` leaves the tree and events untouched; over-budget trajectory payloads are rejected, not truncated; `DeleteCapability`/`DeleteAgent`/`MergeScenes` report unknown ids instead of silent no-ops; reads surface transient errors instead of skipping records<br>4. **Graph import closed loop**: skipped re-imports rebuild their edges, existing graph slots are reused so renames survive re-import, batches validate before writing, `L3Relation.Titles` declares true N-ary hyperedges, `UpdateL3` name collisions are rejected with deterministic re-export routing, deleting a graph detaches scene anchors, `SyncPlanTree` branch deletion mirrors into the trajectory index<br>5. **Format**: this round changes derivation and validation, not the record layout; MCP tool set unchanged (31); every fix re-verified by importing this repo into its own L3 hypergraph — full record in CHANGELOG<br>6. **L3 knowledge graph is a file-wide shared pool**: one file, one shared graph for every agent domain — records live in the reserved shared domain `core.SharedPoolAgentID` (invisible to hosts, spared by `DeleteAgent`), scene anchor validation reads the pool, and `DeleteL3` detaches scene anchors across domains in a second lock-free phase<br>7. **L5 unified card + file-wide capability pool + `plug/` auto-injection**: one card form — name + N function entries, no card-level `Type`/`Workflow`, action chains live in a `composite` entry's `config` as `{"steps":[{"tool":...}]}`; `memhop-capability/v4` package documents (one file = one plugin package, the package name stamps each card's `Package` field); `ImportCapability` returns per-card `created/updated/errors`, `CapabilityListQuery` gains a `Package` filter; L5 records join the same shared pool; every Open auto-imports `<meh-dir>/plug/<package>/capability.json` (broken packages Warn and skip); format 0x0009 → 0x000B, files older than 0x000B rejected at Open, no migration|
| v1.5.0 | 2026-09-01 | L2 re-shape: a scene IS a host session, and the library owns the turn id | 1. **`Search` reads the scene AND opens the turn**: input `{scene_id, l3_id}`, both optional — an empty `scene_id` mints a scene (library-named `session:<id>`; the `scene_name` input is gone — the host titles a scene with `UpdateScene`), a non-empty unknown one is `ErrNotFound`, and `l3_id` only anchors a newly created scene. The result is `{profile, profile_brief, scene, topics, new_topic_id}`: the scene record, its depth-1 topics in turn order (the context to inject) and the topic id this read minted for the turn about to run, `hash("turn:" + scene:turn_seq)` from a new per-scene `turn_seq` counter. `contexts`/`associated_contexts`/`auto_create`/`directed_l2_id`/`directed_l3_id`, the `ctx` parameter and every read-path LLM/embedding/scoring call are gone. That counter write is the read's only write and is now load-bearing: a failed write fails the read instead of reissuing a possibly duplicate id<br>2. **`Update` settles the whole turn into that id**: `TurnUpdate{scene_id, topic_id, user_text, user_ts, user_type, agent_text, agent_ts, agent_type}` returns the same id; one distillation runs before any write, so a failed LLM call leaves nothing behind; a missing / zero / non-hex `topic_id` is `ErrInvalidQuery`; replaying one id overwrites the turn and tombstones the two archives it supersedes. Both timestamps are now ordering fields only, not identity inputs<br>3. **N:N append surface deleted**: `AppendL4Message` (many messages into one topic) and `RefineTopicKeywords` (re-distill one topic from all originals) are gone — a turn's L4 originals are exactly its two texts, and what happens between them belongs to the turn's L6 trajectory. L4 content types are declared on the way in — `user_type`/`agent_type` (zero value = `text`, a non-text slot carries its media path or URL as the text) — and `SceneMessage.type` reports them back<br>4. **L6 trajectory keyed by the turn's topic id**: `AppendTrajectory` / `ReadTrajectory` / `Crystallize` take that id and the event's `topic_id` is stamped from the key, so host-minted turn keys disappear; plan-bound events keep using the plan id, and the cross-turn fold (with `TrajIndex.TopicEvents`) is deleted — a plan id is now the cross-turn aggregation unit<br>5. **Single keyword track**: `user_keywords`/`agent_keywords`/`centroid_page_ref`/`l3_refs` removed, only `fused_keywords` remains (same on-disk field name); Dream compression, L1 hyperedges and host injection all read that one track, no summary field — originals stay in L4<br>6. **Scene ids are minted by the library**: `NewSceneSlot(sceneID, name)` no longer hashes the name; `CreateSceneL2WithID` reuses an existing scene idempotently; the `timestamp:text` auto-naming path disappears<br>7. **Retrieval subsystem deleted**: `internal/cap/scenefind` (three channels, RRF, scene bonuses, L1 spreading activation), topic centroids, `RecVecCentroid`, `Encoder`/`HttpEncoder`/`OpenMultiWithEncoder` and the encoder config — **the engine contacts no embedding service**<br>8. **`VectorDim` deleted** from the config surface (`CheckVectorDim`/`ErrVectorDimMismatch`/MCP `--vector-dim` with it); the two header bytes at offset 6 are reserved, format stays `0x0009`<br>9. **Dead index island deleted**: the orphaned BM25 / entity / BK-tree / L3-index code (L3 node search always scanned records), two Levenshtein implementations and zero-caller `common.FormatIDs`<br>10. **Consolidation trigger re-axled**: `activeScenes`/`Capacity` plus `ActiveSceneIDs`/`HasActiveScenes` removed; `Update` schedules a scene's Dream once its depth-1 count passes `SceneDreamTopicThreshold` (default 24); `Dream(ctx, "")` sweeps every scene of the domain<br>11. **No format bump**: `TopicSlot.UnmarshalJSON` folds an old file's two tracks into `fused_keywords` at decode time, and `turn_seq` is additive (scenes written before this release decode to 0 and open turn 1 on the first read). Turn topics derive their id under a `"turn:"` namespace disjoint from Dream's fused-node ids; the unused `ComputeTopicIDForText` and the dead `SceneNode.VectorPageRef` are removed<br>12. Scene merging no longer happens inside Dream (it would delete a sceneID the host still holds); `MergeScenes` stays an explicit host API. **Delivery**: MCP `memhop_search` loses `scene_name` and returns `new_topic_id`, `memhop_update` requires `topic_id`, the trajectory tools key on that id, `memhop_status` reports `scene_count`, `memhop_scene_active_list` is deleted while `memhop_scene_rename` is added (30 → 31 tools); **L0 profile field ownership is enforced by the library** — `UpdateL0` writes only the host's four fields, inherits the Dream-evolved emotion state and MBTI from the stored profile and stamps `updated_at_ms` itself, so `memhop_profile_update` is a plain forward and Go hosts get the same guarantee; `SyncPlanTree` inherits a blank `Title`/`PlanType`/`Status`/`Summary` instead of rewinding the node, a failed Dream merge group rolls back what it wrote, and a transient read failure is never reported as "record not found"; the DSH plugin surface (`dsh/`, `dsh-adapter/`) is retired in this same release, so the delivery is the library plus the MCP server<br>13. **Public surface consolidated to what a host actually reaches** (audited against `api/` alone — the MCP tool set is not a reason to keep a method): Session 43 → 33 methods, `MultiAgentDB` 9 → 7; the post-release interface audit below added `DeleteL3Nodes` and `CompactTo`, so the surface is 34 + 8. Deleted with their implementation chains: `Lock`/`Unlock` (the default domain's mutex exposed to hosts), `Session.Checkpoint`/`IsClosed`/`AgentID` (duplicates of the DB handle), `DistillL0` (a Dream stage, not an entry point), `ListPlans` + `plan.Summarize` + `PlanCache.All` (recovery is `PlanState` on a known id), `GetArchive` and `GetCapability` (both are one condition of the list query), `MergeScenes`' dead repo overwrite twin `OverwriteSceneL3ID`, and the un-consumed `api.CapabilityImport` alias. Merged: `ListScenesByL3` → `ListScenes(l3ID)`, `SetSceneName`+`SetSceneL3ID` → `UpdateScene(id, ScenePatch{Name, L3ID, Force})` (one read-modify-write), `PlanAppend` → `AppendTrajectory(key, nodePath, ev)` (empty nodePath = the bare turn event). **The library issues every id**: the four `Format*`/`Parse*` bridges are gone — `api.DefaultAgentID` names the implicit domain and `api.NewPlanID(name)` mints a plan id under a `plan:` namespace, so a host echoes ids and never builds one; `internal.FormatAgentID`/`ParseAgentID` collapsed into the one `FormatID`/`ParseID` pair. A fourth was caught by a new reflection guard: `UpdateScene` had been merged without a facade override, so embedding leaked `core.SceneSlot` to hosts with uint64 ids — the facade now maps it and hands back the **written scene** (confirming an anchor no longer costs a `ListScenes` sweep), and `api/surface_public_test.go` rejects any host-visible uint64 id field. `api.NewPlanID` borrows away from the reserved 0, so every id the library issues is one it accepts. Three silent failures fixed: `SearchL4` with only `TopicID` or only `Type` returned nothing (the L4 selector is now a set of AND-ed conditions, so one call pulls a turn's originals); re-anchoring an anchored scene answered `nil` while changing nothing (now `ErrInvalidQuery` unless `Force`, and the target domain must exist — also enforced when `Search` anchors a new scene); `Dream` on an unknown scene id returned a zero report with no error (now `ErrNotFound`)<br>14. **Post-release per-layer interface audit** (every finding re-checked against source, plus the public surface imported into this repo's own L3 hypergraph and queried): two core-layer defects that turned a wrong id into silent data loss are fixed — **typed record reads now check the frame's record type** (`GetL3(nodeID)` and friends answer `ErrNotFound` instead of decoding a foreign slot, which is what let `UpdateL3` rename a node record into a graph slot), and **a hyperedge's identity includes its kind** (an edge pair carrying both `related` and `part_of` used to collapse to whichever kind was written last; re-imports dedupe on sorted members + kind, so edges already stored under the pair-only hash are not duplicated). The public surface got more honest, in the host's favour: the twelve contract-heavy methods that reached hosts by embedding alone are now **declared with doc comments on the facade** (`go doc api.Session` showed 22 of 34 before), `ImportL3` reports `graph_ids` for the graphs it wrote (a graph id is `hash(Domain)` and no other public call renders that derivation), and two new entries exist: `DeleteL3Nodes` (node-level deletion, cascading its hyperedges) and `MultiAgentDB.CompactTo` (the way back from tombstone-only deletes). Semantics tightened or made consistent: `Update` may only settle **a turn this scene opened** (writing a Dream-fused topic, another scene's turn, or a host-invented id is refused, while replays and out-of-order settles stay valid); `QueryL3Nodes` conditions AND together instead of a priority switch that silently ignored two of them; `SearchL4`'s keyword is case-insensitive like the L3 filter and gains `Limit` (newest matches), with the MCP tool capped at 50 by default; `SceneContext`'s depth ≤ 2 flattening is documented as deliberate — it is the only read that brings back the originals Dream sank. Dead public fields removed: `HypergraphNode.importance`, `HypergraphEdge.weight/label`, `ArchiveSlot.metadata` and the `RoleSystem` constant (no write path anywhere in the engine; the record fields stay so older files decode), and `plan_type` is now cleared on event writes since the record contract makes it node-only. Security and delivery: `memhop_capability_import` paths are anchored through `os.Root` on `--capability-dir` (default `--db-dir`) and escapes are refused; `memhop_dream`'s `scene_id` is optional so MCP hosts can reach domain-wide consolidation at all; the import tool description and the `ListL3` card each claimed something the code does not return and now tell the truth; each card's summary maps its Go method names to the `memhop_*` tools an MCP client actually calls; and the ~70-line `idsToHex` rewrite in the MCP server, a no-op since the api DTOs render every id as hex (four of its fifteen keys named fields no public DTO has), is deleted. New drift guards tie hand-written vocabularies to the engine enums they enumerate. **Format version stays 0x0009**: this round changes derivation and validation, not the record layout. |
| v1.4.2 | 2026-08-31 | L6 plan tree + L2 directory anchor | 1. L6 carries a task tree: `TrajectorySlot.NodeType` splits turn events from plan nodes, node ids derived stably by `HashPlanNode(planID, nodePath)` under a `plan:` namespace, events bound via `PlanNodeRef`<br>2. three-form surface `PlanAppend` / `PlanCommit` / `PlanState` plus `PlanReplace` (re-plan, keeps planID), `SyncPlanTree` (whole-tree snapshot diff, emits no `plan_step`), `ListPlans` (restart recovery)<br>3. **Model A fold**: a parent turns `done` only on an explicit host commit; after each commit the `done` children's summaries roll up bottom-up in numeric `NodePath` order without clobbering a host-written parent summary<br>4. `PlanTree.Roots` is a **forest** (one root per top-level step; orphaned nodes surface as roots instead of vanishing)<br>5. L2 scene → L3 directory anchor (N:1): `SceneSlot.L3ID`, optional `SearchQuery.L3ID` pre-filter with backfill on hit, `ListScenesByL3`, `SetSceneL3ID(sceneID, l3ID, force)` write-once unless correcting or clearing<br>6. per-domain `planCache` so `PlanState`/`ListPlans`/rollup stop scanning the engine per call<br>7. api constants exported: `Role*`, `NodeType*`, numeric `Status*` (read-side), string `PlanStatus*` + `PlanStatus` type (write/query side); fifth status `running` added<br>8. write surface forced authoritative: all plan-node fields and `Seq` are overwritten on every append path, `EventType` restricted for plan events<br>9. hardening: `0000000000000000` is the reserved bare-event `PlanID` and rejected by all five plan entry points (`PlanReplace` on it used to delete every turn event of the domain); Dream's plan exemption narrowed to plans active inside the 7-day window, so abandoned plans no longer accumulate forever<br>10. no format change (stays `0x0009`, additive JSON fields, v1.4.1 files open as-is), MCP tool set unchanged (31) — the plan surface is **Go module only** this release |
| v1.4.1 | 2026-08-28 | Type-contract cleanup: hex-ID DTOs, L0 profile v2, L3 hypergraph activation | 1. api response DTOs are real structs — every ID field leaves as a 16-char hex string (`SearchResult.NewTopicID`, `AppendL4Message`, `AgentID()` included) with new `api.FormatID` / `api.ParseID` helpers<br>2. L0 profile v2 (`FormatVersion 0x0009`): field ownership (Name/Role/Preferences host-exclusive, Personality host-seeded + Dream-distilled), typed `EmotionState`/`MBTI` distillation signals, dead `lexicon`/`style_traits` removed<br>3. zero in-library hex round-trips (repo-layer ID params are uint64, centroid hash via `HashBytes`)<br>4. L3 import gains `source_ref` (positional reference) and `related` (same-graph hyperedges resolved by title, two-phase forward references, idempotent re-import; `edges_created` result field, `L3Relation` type exported)<br>5. `AppendL4Message` gains `contentType` (Content* constants exported; text/document/code carry the original text, image/audio/video carry a path/URI with mime/size/sha256 in Metadata), `L4Query.Type` filter and MCP `archive_search` `content_type` param<br>6. L6 one-trajectory-per-turn: SessionID is a turn key (search opens, update closes), events carry `TopicID` for cross-turn crystallization, external surface trimmed to append/query (`TrajectoryStats` / `DeleteTrajectory` / `PruneTrajectory` removed, 33 → 31 tools), Dream `l6_prune` auto-drops events older than 7 days<br>7. distill/consolidate LLM parsing gains a format-constrained retry<br>8. **breaking**: `.meh` files with `FormatVersion != 0x0009` (i.e. ≤ 0x0008) are rejected at Open, no migration |
| v1.4.0 | 2026-08-26 | Multi-agent memory database | 1. one `.meh` file carries many isolated agent domains: record frames gain `agent_id` (26-byte header), engine indexes and snapshots (0x02) are per-agent, tenant registry records map names to stable crypto/rand agentIDs<br>2. `api.OpenMulti` / `AgentSession` / `CreateAgent` / `ListAgents` / `DeleteAgent`; `Open` stays zero-change for single-agent hosts (default domain)<br>3. business layer rebuilt around per-agent `agentContext` with domain locks (same-agent serial, cross-agent parallel), idle-domain memory reclamation and scoped Dream pipelines<br>4. L7 trajectory layer renumbered to **L6** (cognitive layers converge to L0–L6)<br>5. MCP registry shares one `MultiAgentDB` (one `<db-dir>/memhop.meh`), `os.Root`-anchored db-dir<br>6. duplicate structs/conversion layers removed (`topicSlotJSON`, `topicToL2Meta`, single-value slice wrappers)<br>7. Go 1.23–1.26 stdlib modernization (`iter.Seq2`, `unique.Make`, `os.Root`)<br>8. zero new dependencies<br>9. **breaking**: `.meh` files with `FormatVersion <= 0x0007` are rejected at Open, no migration; promoted `internal.DB` methods on `api.DB` now carry an `agentID` parameter (facade methods unchanged), `Lock()` panics on a closed DB |
| v1.3.4 | 2026-08-26 | L5 tool-declaration isomorphism | 1. `memhop-capability` format v3: `ResourceRef` renamed `description` → `desc` and gained `input` (JSON Schema string) / `output` — the tool-declaration fields now mirror the host tool spec shape (meowire `ToolSpec`) exactly, so hosts project capabilities with a pure field copy and zero format conversion<br>2. `WorkflowStep` gained `args` — action chains carry step parameters officially (no private config formats)<br>3. crystallize prompt emits the v3 shape (`type`/`resources` instead of `kind`/`manifest`)<br>4. `validateCapabilityImport` now requires resource names and validates `input` as JSON<br>5. **breaking**: v2 cards are rejected at import (format must be `memhop-capability/v3`); stored capability records written by earlier versions lose `desc/input/output` on read<br>6. built-in capability toolbox (`capabilities/*.json`) fully rewritten to v3 with real JSON Schemas |
| v1.3.3 | 2026-08-26 | Retrieval scoring normalization + defaults slimdown | 1. vector floor fixed from overriding every other signal to lifting only below-threshold scenes (floor = threshold + cosine×0.5): real-signal ordering (RRF + keyword overlap + bonuses) wins, semantic fallback preserved<br>2. `MemHopDefaults` slimmed from 24 fields to 3 business knobs (`Capacity` / `DreamCompressMinTopics` / `SearchDreamContextThreshold`); 4 dead fields (`MaxResults` / `DefaultTimeoutSecs` / `DefaultMaxOutputTokens` / `MaxDepth`) removed and 16 tuning constants moved to package-private `internal/tuning.go`<br>3. `TopScene` / `SpreadingActivation` / `applySceneBonuses` / `rrfFuse` signatures dropped the defaults parameter<br>4. **breaking**: hosts referencing removed fields must clean up<br>5. no format change (stays `0x0007`)<br>6. MCP tool set unchanged (32)
| v1.3.2 | 2026-08-26 | API fixes: async Dream + deletion + Update simplification | 1. Search/Update no longer block on an internally triggered Dream (background goroutine, per-scene in-flight dedup, Close cancels a pending Dream)<br>2. new `DeleteTopic` (subtree closure + L4 + indexes + parent ChildrenIDs pruning) and `DeleteScene` (scene + all topics + archives + L1 node + active set) for memory correction<br>3. `Update` returns `error` instead of `(bool, error)`<br>4. `SearchResult.ProfileBrief` — compact profile digest (name/role/top preferences/style/emotions, bounded)<br>5. no format change (stays `0x0007`)<br>6. MCP tool set unchanged (32)
| v1.3.0 | 2026-08-26 | L1 scene hypergraph + spreading-activation association | 1. Dream creates real `RecL1Hyperedge` co-occurrence edges between scenes (keyword-overlap Jaccard ≥ `L1EdgeMinSimilarity`); Search `AssociatedContexts` replaced the no-op same-scene listing with a graph walk (activation × edge weight × dampening per hop, ≤ `L1EdgeMaxHops`, top `L1AssocMaxScenes` other scenes)<br>2. L6 scene-usage record removed — hit counters folded into the L2 `SceneSlot` (`HitCount`/`LastHitAt`)<br>3. `L1ReverseIndex` (incl. snapshot field) and 4 dead L1 functions removed; association is now a pure storage-level graph read<br>4. `.meh` format bumped to `0x0007` — 0x0006 files are rejected at Open, no migration<br>5. new defaults: `L1EdgeMinSimilarity` (0.15), `L1EdgeMaxHops` (2), `L1ActivationDampening` (0.5), `L1ActivationThreshold` (0.05), `L1AssocMaxScenes` (3)
| v1.2.7 | 2026-08-25 | Host alignment + bilingual integration guides | 1. `Search(ctx, q)` and `RefineTopicKeywords(ctx, id)` accept a context (cancels LLM extraction, encoder calls, internally triggered Dream)<br>2. `api` exports `LlmConfig` / `MemHopDefaults` / `TopicSlot` / `ResourceRef` / `CrystallizeDetail` / `TrajectoryStats`<br>3. new `TrajectoryStats` (per-session L7 stats) + `memhop_trajectory_stats` MCP tool (31 → 32 tools)<br>4. `CrystallizeResult.Details` — per-candidate create/reuse/merge/skip disposition<br>5. `AppendL4Message` (pure L4 append, no LLM)<br>6. active-scene capacity: Update triggers a Dream on the oldest scene at Capacity with a compressibility pre-check; `SearchDreamContextThreshold` zero-value guard<br>7. bilingual integration guides added at repo root (`INTEGRATION_GUIDE.md` / `INTEGRATION_GUIDE.zh.md`) |
| v1.2.5 | 2026-08-20 | MCP server rewritten | 1. `cmd/memhop-mcp` fully rewritten against the `api` facade (v1.2.4 removed it): all 31 MCP tools map 1:1 to `api.DB` methods<br>2. multi-tenant HTTP — SSE + streamable-http (2025-03-26 spec, stateless), each tenant isolated by URL path `/mcp/<tenant-id>` into its own `.meh` file, lazy-open registry with a first-open mutex<br>3. all tool outputs serialize record IDs as 16-char hex strings (uint64 JSON numbers lose precision in JS/TS hosts)<br>4. tenant-ID whitelist + path-traversal rejection (defense in depth)<br>5. LLM credentials via env vars only (no CLI flag)<br>6. go-sdk v1.7.0 back as a direct dep (3 → 4)<br>7. offline tests for config/registry/tools/streamable + multi-tenant SSE smoke<br>8. codebase cleanup: dropped redundant enum JSON helpers (default `~uint8` JSON behavior is identical), `CodeOf` migrated to Go 1.26 `errors.AsType`, scalar cosine loop (2.7× faster at 1024 dims), deleted the `internal/repo/open.go` forwarding layer (17 funcs + 8 aliases; internal calls core/index directly), removed the `ParseID→FormatHash` round-trip in Update |
| v1.2.4 | 2026-08-19 | api/ facade + internal/ flattening | 1. Public Go API moved from the root package to `github.com/qyiun666/MemHop/api` (root `memhop.go`/`types.go` removed)<br>2. `internal/sub/` flattened into `internal/` (`package sub` → `package internal`), `internal/sub/repo` → `internal/repo`, `internal/sub/common` → `internal/common`<br>3. `cmd/memhop-mcp` removed (rewritten in v1.2.5)<br>4. build config (Makefile fmt, pre-commit hook, CI gofmt) updated<br>5. breaking change: hosts importing the root package must switch to `/api` |
| v1.2.3 | 2026-08-18 | MCP compatibility fixes + DSH integration + retrieval quality | 1. MCP tool schemas fixed (no-arg tools no longer emit `properties: null`, breaking strict clients)<br>2. all tool outputs render record IDs as 16-char hex strings (uint64 JSON numbers lose precision in JS/TS hosts, breaking `new_topic_id` round-trips)<br>3. new `--transport streamable-http` (2025-03-26 spec, stateless multi-tenant; supported by DSH's dsh-mcp-client)<br>4. DeepSeek Harness integration guide + agent instructions (`docs/dsh/`)<br>5. streamable-http smoke test<br>6. keyword-extraction prompt overhauled (semantic completeness + colloquial variants + phrases) + Search returns all relevance-ordered topics (scene-context truncation removed), LoCoMo recall 0.392 → 0.668, entity_hit 0.284 → 0.877 |
| v1.2.1 | 2026-08-16 | MCP server + L5 capability layer | 1. New `cmd/memhop-mcp` binary: multi-tenant SSE MCP server (official go-sdk v1.7.0) mapping the full public API to 28 tools (search/update/dream/checkpoint/status, profile, scenes, knowledge, archive, capabilities, trajectory/crystallize)<br>2. tenant path isolation `/mcp/<tenant-id>`<br>3. graceful shutdown persists via snapshot<br>4. offline SSE smoke tests (`make test-mcp`)<br>5. usage docs under `docs/mcp/` (local)<br>6. L5 plugin layer refactored into the capability layer (`memhop-capability/v1`: manual/atomic/composite kinds, draft→active lifecycle via `ActivateCapability`, fingerprint dedup, Crystallize emits create/reuse/merge candidates)<br>7. built-in capability toolbox (`capabilities/`, embedded, read-only, attached at Open)<br>8. `Update` returns `(bool, error)`<br>9. `.meh` format bumped to `0x0005` — 0x0004 files (v1.2.0 plugin records) are rejected at Open, no migration<br>10. encoder health check requires a 2xx HEAD on the endpoint root (no fallback)<br>11. active scenes bounded by `Capacity` (default 7, oldest evicted from Dream targets)<br>12. `RecordEnd` header field + A/B header damage recovery |
| v1.2.0 | 2026-08-14 | L5 plugin layer | 1. L5 action chains → plugin slots (PluginSlot + structured five-section manifest: skills / MCPs / tools / prompts / services)<br>2. path-only import via `ImportPlugin`, hand-written create/update removed<br>3. Crystallize dispatches plugins by type from L7 trajectories<br>4. `SearchResult.Crystals` → `Plugins`<br>5. eight-layer architecture (L0–L7) docs |
| v1.1.0 | 2026-07-27 ~ 08.11 | Architecture refactor | 1. Layered `internal` rewrite (assembly → sub → repo → core/index/common)<br>2. f16 → f32 single-precision vectors<br>3. topic centroid vector retrieval<br>4. `BatchStore` removed<br>5. `Dream(ctx)` narrowed to `(bool, error)`<br>6. `.meh` format `0x0004`, incompatible with v1 data<br>7. integration tests rebuilt against the new internal API |
| v1.0.0 | 2026-07-26 | First stable release | Go rewrite with six-layer cognitive architecture, V2 .meh storage, BM25+vector+entity RRF search, Dream consolidation pipeline, L3 hypergraph with community detection. |
| v0.54–v0.58 | 2026-07-16 ~ 07-23 | Go Rewrite | 1. v0.58: Unified RRF — additive scene bonuses, three-channel fusion, L6 removed, atomic.Pointer<br>2. v0.57: Dream narrowed to L0+L1+L2, LLM hardening, L5 Write API, SkipDistill<br>3. v0.55: Stability — IVF removed, panic→error, crash recovery, L5 write pipeline<br>4. v0.54: Go foundation — 4-layer arch, V2 .meh storage, 2 deps, log/slog |
| v0.18–v0.63 | 2026-05-31 ~ 07-10 | Rust | 1. V2 append-only `.meh` with snapshot/checkpoint<br>2. BM25 + IVF hybrid retrieval<br>3. L3 hypergraph DSL, community detection (clique + Louvain), BFS/caching<br>4. Full Dream pipeline: L3 distill → L2 compress → L1 decay → L0 rebuild → L5 crystallize<br>5. FFI (cdylib), MCP Server, gRPC/Unix Socket encoder |
| v0.6–v0.17 | 2026-05-20 ~ 05-25 | Rust Early | 1. Pure Rust single crate (dropped Python bindings)<br>2. LMDB to custom `.meh` storage migration<br>3. 4-layer to 6-layer cognitive architecture evolution<br>4. MCP Server integration<br>5. HNSW vector index (replaced brute-force) |
| v0.1–v0.5 | 2026-05-19 ~ 05-24 | Python | 1. Hopfield associative memory network<br>2. LMDB embedded storage, `pip install` one-click<br>3. O(1) associative recall with confidence scoring<br>4. BrainLoop self-circulating agent loop<br>5. Proved "living memory" concept |

## Links

| | |
|---|---|
| MeowAgent | [github.com/meowagent/meowagent](https://github.com/meowagent/meowagent) — coming soon |
| MemHop | [github.com/qyiun666/MemHop](https://github.com/qyiun666/MemHop) |
| Meowire | [github.com/qyiun666/meowire](https://github.com/qyiun666/meowire) |
| MeowDesk | [github.com/qyiun666/MeowDesk](https://github.com/qyiun666/MeowDesk) — coming soon |
| Website | [qyiun666.github.io/meowagent.github.io](https://qyiun666.github.io/meowagent.github.io/) |
| Email | qyiun666@163.com |

<p align="center">⭐️ <a href="https://github.com/qyiun666/MemHop">Star MemHop on GitHub</a> — your support keeps us building!</p>

## License

MIT OR Apache-2.0
