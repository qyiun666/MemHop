# MemHop v0.18.0 设计评审报告

**评审人**：沈必精 (precise-reviewer)
**评审日期**：2025-07-01
**评审范围**：架构设计文档 (jiagou.md) + Agent 集成文档 (AGENT_INTEGRATION.md)

---

## 一、问题汇总

| 级别 | 问题类型 | 数量 |
|------|---------|------|
| 🔴 阻塞级 | 术语不一致、逻辑矛盾 | 5 |
| 🟡 建议级 | 遗漏、结构问题、细节缺失 | 8 |
| 🔵 确认级 | 需要产品决策 | 3 |

---

## 二、阻塞级问题（必须修改）

### 问题 1：编码器描述与 Stats 返回值矛盾

**位置**：
- 架构设计文档 §2（第14行）、§4（第63行）
- Agent 集成文档 §3.5 memhop_stats 响应示例（第256-257行）

**问题描述**：
架构设计文档明确说明 v0.18.0 使用 `intfloat/multilingual-e5-small` **单一中英双语编码器**（384维），但 `memhop_stats` 响应示例显示：
```json
"encoder_mode": "dual-bge",
"encoder_dim": 512
```

这是旧版（v0.18.0）的双 BGE 编码器配置，与 v0.18.0 的架构描述完全矛盾。

**影响分析**：
- 开发者会困惑于实际使用的编码器是哪个
- Agent 层根据 `encoder_mode` 和 `encoder_dim` 做适配时会产生错误
- 环境变量 `MEMHOP_MODEL_PATH` 的含义不一致

**修改建议**：
将 memhop_stats 响应示例修改为：
```json
{
  "encoder_mode": "multilingual-e5-small",
  "encoder_dim": 384
}
```

---

### 问题 2：接口文档章节编号重复

**位置**：Agent 集成文档（第30行、第62行）

**问题描述**：
文档中有两个"三"：
- **三、接入规则**（第30行）
- **三、接入格式与返回格式**（第62行）

**影响分析**：
- 文档结构混乱，读者无法准确引用章节
- 后续"四、错误处理"和"五、Agent层适配指南"的编号也受到影响

**修改建议**：
调整章节编号：
- 三、接入规则
- 四、接入格式与返回格式
- 五、错误处理
- 六、Agent 层适配指南

---

### 问题 3：接口列表不对齐

**位置**：
- 架构设计文档 §14（第153-165行）
- Agent 集成文档 §一（第3-17行）、§五（第310行）

**问题描述**：
两份文档的接口列表存在差异：

| 架构设计文档接口 | Agent 集成文档对应接口 | 状态 |
|----------------|---------------------|------|
| 对话接口 | memhop_recall | ✅ 有对应，但名称不一致 |
| Dream | memhop_dream / memhop_consolidate | ✅ 有对应 |
| Feedback | memhop_feedback | ✅ 有对应 |
| Stats | memhop_stats | ✅ 有对应 |
| 已激活上下文 | memhop_get_activated | ⚠️ 架构文档未列出具体方法名 |
| L3 路径 | memhop_list_l3_paths | ⚠️ 架构文档未列出具体方法名 |
| L4 原文 | memhop_get_l4_raw | ⚠️ 架构文档未列出具体方法名 |
| 更新接口 | memhop_batch_store | ⚠️ 架构文档未列出具体方法名 |
| 重新适配 | memhop_re_search | ⚠️ 架构文档未列出具体方法名 |
| — | memhop_organize | ❌ 架构文档遗漏 |
| — | memhop_mount_shelf | ❌ 架构文档遗漏 |
| — | memhop_unmount_shelf | ❌ 架构文档遗漏 |
| — | memhop_health | ❌ 架构文档遗漏 |
| — | memhop_get_profile / memhop_set_profile | ❌ 架构文档遗漏 |

**影响分析**：
- 开发者无法从架构文档获得完整的接口清单
- 架构文档的接口描述过于抽象，无法直接映射到实际 API

**修改建议**：
1. 架构设计文档 §14 改用具体的方法名，如 `memhop_recall` 而非"对话接口"
2. 补充遗漏的接口：`memhop_organize`、`memhop_mount_shelf`、`memhop_unmount_shelf`、`memhop_health`、`memhop_get_profile`、`memhop_set_profile`
3. 或在架构文档中注明"完整接口列表见 AGENT_INTEGRATION.md"

---

### 问题 4：memhop_recall 参数定义与返回值不完整

**位置**：Agent 集成文档 §3.2（第155-197行）

**问题描述**：

**4a. query 参数必填性存疑**
`query` 标记为"否"（可选），但作为检索接口，没有 query 时的行为未说明：
- 是返回全部结果？
- 还是返回最近的结果？
- 或者会报错？

**4b. 返回值字段缺少说明**
响应中包含以下字段，但参数表格中未说明：
- `activated_topics`：类型？含义？
- `l0_profile`：类型？何时有值？
- `confidence`：取值范围？

**影响分析**：
- Agent 层开发者不知道如何处理 query 为空的情况
- 返回值字段含义不明确，需要猜测或查看源码

**修改建议**：
1. 明确 query 为空时的行为（如：返回按时间排序的最新结果）
2. 补充返回值字段的类型和说明表格

---

### 问题 5：Dream 接口返回值与架构描述不匹配

**位置**：
- 架构设计文档 §6（第84-92行）
- Agent 集成文档 §3.3（第200-207行）

**问题描述**：
架构文档描述 Dream 为 **7 阶段巩固管线**，但 `memhop_consolidate` 响应只有：
```json
{
  "chains_consolidated": 0,
  "topics_merged": 0,
  "duration_ms": 42,
  "vitality_decayed": 3,
  "schemas_emerged": 0
}
```

这 5 个返回字段无法对应 7 个阶段，且缺少以下阶段的统计：
- NREM（超边权重衰减）→ 可能对应 `vitality_decayed`
- REM（话题合并）→ 对应 `topics_merged`
- REM（话题反思）→ 无对应字段
- REM（计划压缩）→ 无对应字段
- 共现分析 → 可能对应 `chains_consolidated`
- L0 形成 → 对应 `schemas_emerged`
- 索引重建 → 无对应字段

**影响分析**：
- Agent 层无法得知 Dream 各阶段的执行情况
- 调试困难，无法定位哪个阶段出现问题

**修改建议**：
考虑扩展返回值，或在文档中说明各字段与阶段的对应关系。

---

## 三、建议级问题（可选优化）

### 问题 6：版本号格式不一致

**位置**：
- 架构设计文档标题：`(v0.18.0.0)`
- Agent 集成文档标题：`MemHop v0.18.0`

**问题描述**：版本号格式不统一，一个带 `.0`，一个不带。

**修改建议**：统一为 `v0.18.0.0`。

---

### 问题 7：Agent 层配合清单缺少接口映射

**位置**：架构设计文档 §15（第167-177行）

**问题描述**：
清单中的功能名称与实际接口方法名不一致：
- "Store" → 应为 `memhop_batch_store`
- "Re-search" → 应为 `memhop_re_search`
- "Feedback" → 应为 `memhop_feedback`
- "Profile" → 应为 `memhop_get_profile` / `memhop_set_profile`

**修改建议**：在清单中添加具体的接口方法名。

---

### 问题 8：Feedback 接口参数说明不详细

**位置**：Agent 集成文档 §3.4（第229-233行）

**问题描述**：
1. `result_ids` 说明为"recall 返回的结果 ID 列表"，但未说明：
   - ID 格式（是 `kn_` 前缀的字符串？）
   - 是否必须是最近一次 recall 返回的 ID？
   - 可以跨会话反馈吗？

2. `relevant` 的效果描述在"响应"下方（第241行），而非参数表格中。

**修改建议**：
1. 补充 `result_ids` 的详细说明
2. 将 feedback 效果说明移至参数表格或独立小节

---

### 问题 9：缺少 Profile 接口的详细定义

**位置**：Agent 集成文档 §五（第310行）

**问题描述**：
文档提到 `memhop_get_profile` 和 `memhop_set_profile` 无需改动，但完全没有接口定义（请求/响应格式）。

**影响分析**：
新接入的 Agent 开发者无法了解如何使用 Profile 接口。

**修改建议**：
补充 Profile 接口的请求/响应示例，或注明"详见其他文档"。

---

### 问题 10：记忆激活机制描述不完整

**位置**：
- 架构设计文档 §8（第103-109行）
- Agent 集成文档 §3.4（第241行）

**问题描述**：
架构文档提到"已激活的上下文设有倒计时"，但未说明：
- 默认 TTL 是多少？
- 超时后的状态是什么？

Agent 集成文档提到 feedback 可调整 TTL (+60s / -60s)，但：
- 基础 TTL 值未说明
- TTL 的最小/最大限制未说明

**修改建议**：
在架构文档中补充激活机制的详细参数。

---

### 问题 11：长文本处理策略描述模糊

**位置**：架构设计文档 §12（第136-141行）

**问题描述**：
文档描述长文本处理为"按段落或内容语义切断"，但：
- 未说明最大长度阈值
- 未说明切断策略（按段落？按句子？按 token 数？）
- 未说明分批编码时的上下文丢失风险

**修改建议**：
补充长文本处理的具体策略和参数。

---

### 问题 12：memhop_batch_store 参数缺少 domain_id 说明

**位置**：Agent 集成文档 §3.1（第109行）

**问题描述**：
`domain_id` 参数说明为"知识领域 ID"，但：
- 未说明 domain_id 的来源（是 `memhop_mount_shelf` 返回的？）
- 未说明不传时的默认行为
- 与 `memhop_mount_shelf` 的关系未说明

**修改建议**：
补充 `domain_id` 的详细说明和使用示例。

---

### 问题 13：检索层 L3 在默认 target_layers 中缺失

**位置**：Agent 集成文档 §3.2（第160行）

**问题描述**：
`target_layers` 默认值为 `["L1", "L2", "L4"]`，不包含 `L3`。

但架构文档描述 L3 为"知识图层"，在 v0.18.0 新增了 HNSW 向量索引。

**疑问**：
- L3 为何不在默认检索范围内？
- 是设计如此（L3 需要显式指定）？
- 还是文档遗漏？

**修改建议**：
说明 L3 不在默认范围的原因，或修改默认值。

---

## 四、确认级问题（需产品决策）

### 问题 14：memhop_consolidate 与 memhop_dream 的关系

**位置**：Agent 集成文档 §一（第9-10行）

**问题描述**：
文档说 `memhop_dream` 是 `memhop_consolidate` 的"别名"，但：
- 两者返回格式是否完全一致？
- 语义上是否有差异（consolidate 可能只做部分阶段？）
- 建议明确说明：调用任一接口效果完全相同

**决策点**：
是否需要保留两个接口？还是废弃其中一个？

---

### 问题 15：confidence 分数的使用建议

**位置**：
- 架构设计文档 §13（第145-151行）
- Agent 集成文档 §3.2（第196行）

**问题描述**：
文档描述了 confidence 的三因子模型，但未说明：
- Agent 层应如何使用 confidence？
- 是否有推荐阈值（如 confidence < 0.5 时应重新检索）？
- confidence 与 re_search 的触发关系？

**决策点**：
是否需要在文档中提供 confidence 的使用建议？

---

### 问题 16：importance 的初始值建议

**位置**：
- 架构设计文档 §10（第121行）
- Agent 集成文档 §3.1（第106行）

**问题描述**：
importance 默认值为 0.5，但：
- Agent 层应如何判断记忆的重要性？
- 是否有推荐的 importance 分级（如：关键事实=1.0，一般对话=0.5，闲聊=0.2）？
- 不同类型记忆的推荐值是什么？

**决策点**：
是否需要提供 importance 的使用指南？

---

## 五、文档结构问题

### 问题 17：两份文档定位不清晰

**当前状态**：
- 架构设计文档：偏概念设计，接口描述抽象
- Agent 集成文档：偏实现细节，接口定义具体

**问题**：
1. 开发者需要同时阅读两份文档才能完整理解系统
2. 架构文档的"对外接口"章节与集成文档的接口列表重复但不一致
3. 架构文档缺少可操作的接口调用示例

**建议**：
1. 明确两份文档的定位：
   - 架构文档：面向架构师，描述设计理念和整体结构
   - 集成文档：面向开发者，提供完整的接口定义和使用示例
2. 架构文档的接口章节改为"接口概览"，指向集成文档获取详细定义
3. 避免在两份文档中重复定义相同内容

---

## 六、评审结论

**评审结果**：🔶 有条件通过

**通过条件**：
1. 修复 5 个阻塞级问题（特别是编码器描述矛盾和接口列表不对齐）
2. 补充 memhop_recall 返回值说明
3. 统一版本号格式

**建议优先级**：
1. 🔴 立即修复：问题 1（编码器矛盾）、问题 2（章节编号）、问题 3（接口列表）
2. 🟡 开发前修复：问题 4（recall 参数）、问题 5（Dream 返回值）
3. 🔵 可后续优化：问题 6-17

---

**评审人签名**：沈必精
**日期**：2025-07-01
