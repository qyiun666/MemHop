# MemHop Benchmark Dependencies
# ============================================================

# Embedding model
sentence-transformers>=3.0.0

# Competitors
faiss-cpu>=1.8.0          # FAISS (CPU version for fair comparison)
chromadb>=0.5.0           # ChromaDB
pymilvus>=2.4.0           # Milvus Lite

# Data processing & config
pyyaml>=6.0               # config.yaml parsing
numpy>=1.26

# Progress & reporting
tqdm>=4.66                # progress bars
tabulate>=0.9             # pretty tables in reports

# Optional: C-MTEB datasets
mteb>=2.0.0               # MTEB framework (includes C-MTEB)

# Optional: BEIR datasets
beir>=2.0.0               # BEIR framework
